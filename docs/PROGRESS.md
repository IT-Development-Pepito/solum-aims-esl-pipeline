# Current Phase

The **engineering phase began on 2026-08-25** after the dedicated local non-production PostgreSQL test connection was verified. Foundation workflow, validation, promotion-evidence, action-lifecycle, audit/reconciliation, health/configuration, safe retention/audit-read, promotion compatibility selection, failure classification and bounded retry, AIMS adapter boundaries, recurring schedules with auditable manual launch, per-scope launch ownership, and operator status, retry, and bounded replay controls work through PR #84 are merged into `develop`. A local clone of both AIMS databases now exists, so vendor-facing work can proceed without touching production AIMS. The automated CSV compatibility contract remains approved but its follow-on implementation is deferred until its foundation dependencies and the consumer acceptance gate are complete.


## Cross-agent implementation checkpoints

Every checkpoint records these fields.

| Field | Required content |
| --- | --- |
| Timestamp and owner | Local timestamp and agent/human identifier. |
| Issue | GitHub issue number, title, labels, and assignee. |
| Git state | Branch, worktree path, HEAD SHA, merge/PR state, and uncommitted-file status. |
| Scope | Requirement/rule IDs, completed behavior, explicit non-goals, and files changed. |
| Evidence | Exact test/lint/type/build commands, results, and environment version. |
| Configuration | Variable names and secret-storage location only; never values, tokens, URLs containing passwords, or DPAPI blobs. |
| External state | Read/write action taken against PostgreSQL, SQL Server, AIMS, filesystem delivery, or none. |
| Risks and next action | Open question/blocker, decision owner, and one smallest safe next action. |

Checkpoints live in `docs/checkpoints/`, one file per checkpoint, named
`<YYYY-MM-DD>-<HHMM>-<owner-and-scope>.md`.

There is deliberately **no index list here**. An index is a single shared region that every
issue would edit, which is exactly the merge conflict this layout removes: two agents adding a
checkpoint touch two different new files and never the same lines. The filename ordering is the
chronological ordering, so the most recent checkpoint is the last filename in the directory.

Before starting work, read the most recent checkpoints:

```bash
ls docs/checkpoints | tail -3
```

Add a checkpoint at issue start, after every independently testable task, after a review/fix
round, when blocked, before changing agents or chats, and after merge. Never include credential
values. Do not edit an existing checkpoint: it is a point-in-time record, so correct it by adding
a newer one.

# Completed

- GitHub #14 merged through PR #55 at 228746e: FR-007 explicit workflow states, terminal behavior, dependency conditions, deterministic ordering, and auditable transition decisions.
- GitHub #18 merged through PR #56 at 3317483: durable restart-safe execution state and recovery behavior.
- GitHub #36 merged through PR #57 at b3ae64e: promotion eligibility, UOM, atomic candidate state, and decision evidence; formal winner priority remains unresolved.
- GitHub #12 merged through PR #59 at 10eb6db: structural source validation and safe quarantine; configured range/domain thresholds remain explicitly deferred to blocked discovery issue #58.
- GitHub #19 merged through PR #60 at 19d1641: durable logical idempotency keys, action lifecycle, attempts, and reconciliation-required outcomes.
- GitHub #25 merged through PR #61 at 422a072: reconciliation balance validation plus durable audit, exception, and event evidence.
- GitHub #27 merged through PR #63 at 61aabcb: liveness/readiness/dependency health and versioned configuration validation. Its GitHub scope differed from the AD-016 Task 7 assignment, so the plan's retention and audit-read work was separately tracked rather than silently folded into #27.
- GitHub #62 merged through PR #65 at 6ba029f: disabled-by-default configurable evidence retention guards and sanitized audit read models, completing the remaining AD-016 Task 7 work. Its identified foreign-key purge limitation was closed by #64 through `0009_relax_action_evidence_links`.
- GitHub #37 merged through PR #69 at 873202e: the versioned `compatibility-v1` promotion selection strategy and its two BR-019 ambiguity reason codes, mapped into the record-issue trail. No campaign winner is invented, and deployed parity remains unclaimed pending #38 and representative cases.
- GitHub #20 merged through PR #70 at 7b2c717: the SYSTEM_ARCHITECTURE section 8 failure classification matrix and a bounded retry policy with externalised attempts, timeout, backoff, and jitter, recorded in the sanitized configuration snapshot. An unrecognised dependency and failure pair raises rather than defaulting.
- GitHub #22 merged through PR #71 at 35c2986: FR-018 AIMS adapter ports in `application/contracts.py` with typed outcomes for reconciliation and retry, plus AST-based architecture tests that reject direct AIMS implementation imports. Those tests caught and corrected a real domain-to-configuration layering inversion introduced by #20.
- GitHub #15 merged through PR #72 at cc964f4: FR-008 configured recurring cadences evaluated to the minute in each schedule's own timezone, enable/disable control, and manual launch carrying operator identity and reason, with schedule configuration, enable/disable changes, and launch source all audit-visible.
- GitHub #16 merged through PR #77 at f2f0bc8: FR-011 and FR-012 bounded execution status queries, a failed-only linked retry that refuses an unresolved external action, and an explicit bounded source-window replay, all requiring operator identity and reason and all audit-visible. Implemented by Codex, which stopped at a usage limit after pushing; the pull request was opened and independently re-verified rather than trusted from its checkpoint.
- GitHub #17 merged through PR #73 at 902a77a: FR-009 and FR-017 per-scope ownership taken at launch under the versioned `no-simultaneous-ownership-v1` policy. A contended launch is rejected and creates no execution, and neither a scheduled nor a manual request displaces a live owner.
- AD-016 Task 2 merged through PR #45 at 4ce8eba, closing #13: configuration, canonical snapshot, and difference persistence with additive migration 0002_configuration_and_snapshots. GitHub Actions verified the branch (Ruff, mypy for 16 source files, pytest 55 passed / 12 skipped without a configured test database, frontend typecheck, Vitest, and Vite build). The same suite reports 67 passed with no skip against the dedicated non-production database, which now rests at 0002.
- Approved authoritative data model merged through PR #41 at 1d1ca8d; root develop is synchronized and post-merge verification passed.
- Inspected all supplied current-system evidence under `docs/sql-server/` and `docs/hop-jenkins-pipeline/`.
- Established the approved target direction: one modular service with durable execution state, rather than a microservice platform or another ETL script.
- Documented a temporary read-only AIMS PostgreSQL compatibility adapter for the first cutover; vendor-supported APIs remain the preferred long-term boundary.
- Created the initial source-of-truth specification, architecture, and operations documents.
- Created the approved foundation-and-shadow implementation plan at `docs/superpowers/plans/2026-08-25-esl-platform-foundation-and-shadow-plan.md`.
- Implemented and independently reviewed Task 1: Python/React/Tailwind project scaffold and CI.
- Implemented and independently reviewed Task 2: validated settings and the production DPAPI secret boundary, including Windows Service identity, known-folder, owner, and ACL safeguards.

# Open Work / Blockers

- Issue #91 is implemented on `codex/91-warehouse-store-discovery` and under review in PR #97. Its typed adapter contract dynamically discovers stores from `DBWH_8555.dbo.DimStore` and reads transaction-level `SNAPSHOT` views of `DimItemMapping` and `FactCampaign` scoped only by store; there is no weaker isolation fallback. The caller's source window and database read timestamp are flat, persistable provenance; unreliable source timestamp columns are raw evidence rather than silent SQL filters. Unit and architecture tests are VERIFIED; live SQL Server integration and whether the controlled database enables snapshot isolation remain UNKNOWN because `ESL_TEST_SOURCE_SQL_URL` was not exported. **Resolved by #93 (AD-020):** a read-only catalog check found `snapshot_isolation_state = OFF` on `PEPITO_HO`, `DBWH_8555`, and `ESL`, and the `SNAPSHOT`-only engine failed its first live read with error 3952. The shared `adapters/sql_server` helpers now read under `ESL_SOURCE_SQL_ISOLATION_LEVEL`, `READ COMMITTED` by default, and the level is recorded in `WarehouseProvenance.isolation_level`; the #91 reader was moved onto those helpers without changing its SQL. A live `DBWH_8555` read under the new default is still to be exercised.
- #93 delivers the read-only `PEPITO_HO` reader for `ITEM_UOM_MAPPING_MST` behind `UomMappingSourceReader`. The read is bounded only by the caller's item set, chunked at 500 codes inside one transaction, because the table is central and has no store column; the procedure's `IUM_UOM_MAP_STATUS = 'O'`, `IUM_MAIN_ITM_BARCODE = 1`, and `IUM_SALES_UOM_FLAG = 1` predicates are business rules and stay in the domain, so closed and non-sales mappings are returned raw and rejected with a reason later. Provenance uses the same flat shape as #91 with the isolation level added. A read-only live check against the configured `PEPITO_HO` source, as the read-only account, returned raw rows for three live item codes under `READ COMMITTED`; no write was attempted, and the "identity cannot write" check was deliberately not run against a production server. UOM selection and non-CLR conversion remain the #36 rules and the open business-discovery gate.
- #92 delivers the per-store iRetail reader and its bounded fan-out. `StoreReader` addresses one store from its `DimStore` row through the #78 `store_target` validation, so an address that is not a bare host and plain identifier is refused before any engine exists, and reads all twelve objects in one transaction bounded only by store code: the campaign tables are joined to `CMP_ORG_DTL` solely for scope, the item and condition masters are read whole because the database is the store, and every procedure predicate (`ITM_STATUS`, `CMP_STATUS`, `CIGD_STATUS`, validity dates, `CMP_TYPE`, PFS, `LOC_CD = '001'`, `STOCK_UPDATED_FLAG IS NULL`, `BSP_PRICE_CATG = '001'`, `BSP_STATUS`) is left to the domain. `StoreFanOut` reads stores under `ESL_SOURCE_STORE_CONCURRENCY` with a per-statement `ESL_SOURCE_STORE_READ_TIMEOUT_SECONDS`, both provisional until baselined (NFR-004), and reports each store individually as read, failed (a #20 signal, never driver text), or skipped; `StoreFanOutReport.dependency_health()` renders one non-required #27 line per store. **Two VERIFIED findings changed #91's contract.** `DimStore` holds 83 rows of which 29 carry NULL or blank `ORG_IP`/`ORG_DB`, including non-store rows such as `Express`; the #91 reader raised `SOURCE_DATA MALFORMED` for the whole directory on the first live call, which would have made discovery unusable. It now reports those rows as `StoreDiscoveryResult.unroutable` with the missing column named, exactly as the procedure skips them, and the fan-out lists them as skipped `UNROUTABLE_IN_DIMSTORE`; a row without a store code is still malformed. Whether an unroutable row is a closed store, a non-store organisation, or a data gap is UNKNOWN / NEEDS-DISCOVERY (`ORG_STATUS` and `MasterStatus` exist but their semantics are not documented). Live, read-only, as the read-only account: discovery returned 54 routable and 29 unroutable rows, and store 084 (`PEPITO_084`) was read completely in 31.5 s under `READ COMMITTED`: 15,438 items, 2,123 campaign headers, 19,028 campaign item details, 12,946 stock rows, 74,924 POS offline movement rows, 21,391 selling prices. That volume is the second finding: the raw `POS_OFFLINE_TEMP_ITEM_MOVEMENT` read is large because the procedure's `STOCK_UPDATED_FLAG IS NULL` predicate is a business rule kept out of SQL; at 4 concurrent stores a full fan-out over 54 stores is roughly seven minutes, acceptable against the 30-minute cadence but a measured baseline (NFR-004) is still owed before that judgement is final. No workflow runner consumes the fan-out yet; wiring it into the scheduled execution belongs with the ingestion path that #21 and the canonical model expect.
- #94 delivers the read-only `tb_ESL` baseline reader behind `LegacyBaselineReader`, completing the four source-tier adapters that superseded #11. It reads one store's legacy rows raw in one transaction under the configured isolation, with the same provenance shape as the source tiers, and is kept a baseline rather than a back door by two guards: `from_settings` refuses to build unless `ESL_SHADOW_MODE` is true, and an import-graph test refuses any other module that imports it, so no ingestion or domain path can reach `tb_ESL`. Comparison semantics, tolerances, and mismatch classification are deliberately not here; they belong to the shadow-mode comparison that follows Task 8 and the #37 deployed-parity evaluation, which must not claim parity until #38 and representative cases are complete. Live, read-only, as the read-only account: store 084 returned 14,610 rows in 0.3 s under `READ COMMITTED`, and every `REDLIST` value was empty, which confirms the dead-code finding of PR #80 against live data. With #91 to #94 merged, #21 loses its adapter blocker; what still stands between the adapters and a running workflow is the ingestion path itself, which fetches the four tiers, applies the domain rules, and writes canonical records, and the AIMS mutation adapter #23.
- #103 delivers the canonicalization step, `application/canonicalize.py`, and the source rules it calls, `domain/source_rules.py`. Every predicate the procedure applied in SQL is now one named, pure, tested domain function, VERIFIED from the decoded text: item status `O` (BR-002), mapping status `O`, the three UOM-mapping flags for the selling barcode, category `001` status `A` for the regular price (BR-006), main location `001` and the pending POS flag for stock (BR-003), campaign header `A` and detail `O`, `CMP_TYPE` 0/1/3 to the structured promotion types (type 2, FREE ITEM, is read but unsupported, as in the procedure), the description-to-group ladder in the procedure's order, `DISC_TEXT` as the last hundred characters kept raw (BR-011), weekday evidence from a RUNNING `FactCampaign` row with absence kept distinct from inactive (BR-017), and the item-master field mapping including the `TRADING` and `00002` flag rules. The step joins one store's four raw reads, evaluates campaigns through the #36 rules and the #37 `compatibility-v1` strategy with no existing state, validates through #12, records every exclusion as an issue naming its rule, orders by canonical key, and hashes with the canonical serializer, so identical input hashes identically. Three things are deliberately not decided. The display page is an injected policy whose default is page 0 with reason `DISPLAY_PAGE_POLICY_UNDEFINED`, because BR-007's page mapping is Hop behaviour and the AIMS page semantics (#24) are UNKNOWN / NEEDS-DISCOVERY; the runner (#102) or a later issue supplies the approved policy. The #37 stability tie-break against the existing `tb_ESL` promotion state is not applied, because the baseline is shadow-only (#94) and passing it in would make the computed state depend on the legacy output; `select_compatibility_state` therefore runs with `existing_state=None` and an ambiguous pair stays unresolved with its code. `MEMBER_PRICE`, `PROD_WEIGHT`, `EARLY_EXPIRY_DATE`, and `NFC_URL` are constants in the procedure (`0` or blank) and are `None` here rather than invented. Also open: `FactCampaign` may hold several rows per campaign and item and the procedure orders them by a date column its text truncates in the supplied copy, so the step takes the first row deterministically and records that as a risk; and the source-level issue classification `SOURCE` is new vocabulary for the evidence store, to be persisted by #104.
- #104 delivers the persist-and-reconcile step, `application/persist_run.py`, which makes one run's #103 output durable through the repositories that already exist and adds no table: one finalized `SOURCE_EXPECTED` snapshot set (#13), the store's differences against its own previous finalized snapshot decided by canonical hash and recorded path by path (#13, BR-018), one processing result with its issues per record (#12) and the promotion evaluation behind it (#36), one `INTENDED` action per changed eligible record (#19), a balanced reconciliation report (#25), and, in shadow mode, the legacy `tb_ESL` baseline (#94) compared record by record with each difference an exception carrying the computed and the legacy values side by side under categories that say *mismatch*, never parity. Idempotency rests on durable state: the execution's own finalized snapshot set is the proof the step already ran, so a restart returns the existing identifiers, and an unfinalized set is reported as an interruption for the runner to reconcile rather than silently written over. Four limits are recorded. A keyless exclusion (an inactive item has no canonical record, and `record_issue` must reference a snapshot, the foreign key is `NOT NULL`) is recorded as a `RECORD_EXCLUDED` execution event and counted as rejected, so it reaches the event log but not the reconciliation exception list; giving it a row of its own would need a migration of its own; #21 used the reserved `0008` slot for the recovery gate and deliberately did not add it. `ACTIVE` mode is refused until the AIMS mutation adapter #23 exists. Two runs over the same source window converge on one logical action by the #19 idempotency key, so a changed record produces a new action only when the window differs, which is what a scheduled cadence guarantees. The baseline comparison covers the fields both sides define (regular price, promotion presence, type and text, barcode, stock); tolerances and what a mismatch means remain UNKNOWN / NEEDS-DISCOVERY under #38, and the exception categories are evidence for the #37 deployed-parity evaluation, not a claim. Three repository additions carry the step: `find_snapshot_set` and `previous_finalized_records` on the snapshot repository, `append_exception` and `latest_report` on the reconciliation repository.
- #102 delivers the workflow runner, `application/runner.py`, and closes the gap every earlier issue left open: a launched execution now runs. `WorkflowRunner.run` takes one execution through the #14 graph, `QUEUED → RUNNING → terminal`, with `RETRY_WAIT` and `RECOVERING` used exactly as the graph allows, and drives six steps in the procedure's own order, `discover`, `read-warehouse`, `read-store`, `read-pepito-ho` (the UOM read is bounded by the store's item set, so the store comes first), `canonicalize` (#103), and `persist` (#104, which finalizes the reconciliation report, so reconciliation is inside it rather than a step of its own). Each step is an `execution_step` row with a `<step>:done` checkpoint; the scope lease (#17) is heartbeated before every step and released at the end; a failure is classified by the #20 matrix, retried under the configured policy with the bounded, jittered delay recorded in a `RUN_RETRY_SCHEDULED` event, or ended in `FAILED` with a terminal reason of the form `step:dependency:kind:class`; an exception no adapter classified is `OPERATOR_ACTION_REQUIRED` and never retried, and no driver text reaches any row. Restart recovery: `recover_all` marks executions the previous process left `RUNNING` as `RECOVERING`, and a run found `RUNNING` by a fresh process is recovered before it resumes. The discovered store entry lives in its checkpoint and the persisted run in the state store, so both resume without repeating work; the raw reads are deliberately not stored (AD-005), so a resume repeats them at the next attempt number, which `execution_step` shows. `runtime/worker.py` is the run loop the #28 host now owns alongside the scheduler: it picks `QUEUED`, `RETRY_WAIT`, and `RECOVERING` executions oldest first under `ESL_WORKER_CONCURRENCY`, pauses and resumes with the service, and on stop waits for the runs in flight up to the deadline and reports whether they finished, which is the "checkpoint in-flight work" #28 left open. `esl-admin runs show` now lists each step's latest attempt, outcome, failure class, and last checkpoint. Live sources are the four adapters built per call, so a rotated credential or a corrected `DimStore` row reaches the next run. Recorded limits: `ACTIVE` mode fails terminally before any step until #23; a retry's delay is honoured by the worker's pick order and interval rather than a stored due time, so a `RETRY_WAIT` execution may run a few seconds early after a restart (closed by #21, which stores and honours `retry_not_before`); the end-to-end shadow run against live sources was not exercised in this issue, only against the dedicated database with fake sources; the owner's `esl-admin serve` exercised it the same day (execution `6b297d1d`, recorded below). The #94 import guard now names `runtime/host.py` as the one permitted importer of the baseline reader, for the shadow comparison only.
- **First live shadow run, 2026-09-03, store 084 (owner's development state store, live sources read-only).** The run reached `canonicalize` in 55 seconds (`read-store` 23 s, `read-pepito-ho` 32 s for 15,444 item codes, 41,772 mapping rows) and failed there after six minutes with `canonicalize:unexpected:ValueError:OPERATOR_ACTION_REQUIRED`. Two defects, both VERIFIED against the live rows and fixed on `develop`: (1) `CMP_ITEM_GRP_DTL` holds exact duplicate rows for 1,098 of 17,991 campaign-and-item pairs, which the procedure's `SELECT DISTINCT` collapses and the step did not, so the #36 evidence refused duplicate candidate ids; identical rows now collapse into one candidate, and rows of one campaign that differ by UOM stay apart as `campaign/UOM`; (2) stock was summed by scanning every stock and movement row once per item, 15,444 items against 88,485 rows, which is the six minutes; rows are now indexed once per store and the domain sum is unchanged. Re-run on the same live rows: 1.2 seconds, 15,011 records from 15,444 items, 433 inactive items rejected, 4,853 promotion evaluations of which 1,209 selected, 938 unresolved, 47 rejected, and 11,125 issues, the largest being 3,891 `OUTSIDE_DATE_TIME_WINDOW`, 2,544 `DISPLAY_PRIORITY_SAME_ECONOMIC`, 1,414 `PFS_EXCLUDED`, 1,173 `WEEKDAY_INACTIVE`, 1,026 `CATEGORY_001_PRICE_MISSING`, 224 `PROMO_PRIORITY_DIFFERENT_ECONOMIC`, and 161 `VALUE_BASED_CONVERSION_REQUIRED`. These are the first measured figures behind the open business gates: 938 items of one store cannot be given a promotion until the winner policy (#38, BR-019), the same-economic term selection, and the non-CLR conversion are decided, and 1,026 items have no active category-`001` price (BR-006). **Second live shadow run, execution `6b297d1d`, 2026-09-03 10:52-10:59, store 084, MANUAL, SHADOW.** All six steps succeeded at attempt 1, `persist` included, and the run ended `SUCCEEDED_WITH_EXCEPTIONS`; this is the first end-to-end exercise of the runner and of the `persist` step and `tb_ESL` comparison on live data. Reconciliation report revision 1, FINALIZED: 15,444 extracted, 433 rejected, 15,011 valid, 47 ineligible, 14,964 eligible, 14,026 intended (a first run has no previous finalized snapshot, so every eligible record counts as changed), 938 unresolved, 848 ambiguous, 0 submitted. 11,434 exceptions in 13 categories; the new ones are the baseline comparison, `LEGACY_BASELINE_ROW_MISSING` 991, `LEGACY_BASELINE_ROW_ONLY_IN_LEGACY` 596, and `LEGACY_BASELINE_MISMATCH` 277, which are the first measured mismatch evidence for the #37 deployed-parity evaluation and remain evidence, not a parity claim, until #38 supplies tolerances. Two observations: the operator-supplied source window (19:00-19:30 +08:00) lay after the run's start, which the reads ignore but the #19 idempotency keys derive from, so a scheduled run over the real 19:00 window will converge on the same keys; and no CLI yet surfaces the report or its exceptions, which is #109. With this run in place, #21 was selected on 2026-09-03 with its AIMS/API mutation event split to #113.
- BR-005 promotion precedence is on hold pending POS/merchandising decision.
- The GitHub requirement backlog and its later follow-on issues are assigned and tracked through the issue-led `develop` workflow. Implement only a selected, unblocked issue at a time.
- Promotion-rule issue #37 is merged, but it deliberately retains unresolved ambiguity rather than inventing winner priority. #38 and representative cases remain required before any deployed-compatibility claim.
- P0 issue #38 blocks source-adapter parity until SQL-owner review removes or explains the direct procedure self-invocation and supplies non-production boundary evidence.
- P2 issue #58 is blocked until merchandising/POS and data owners provide configured range/domain thresholds and severity policy. P1 issue #21 no longer has an adapter blocker: #11 was superseded by the four source-tier adapters #91-#94, and #20, the #22 ports, and the #24 compatibility reader are merged. Only the AIMS mutation adapter #23 remains, and it gates the AIMS/API recovery scenario alone; the other six named events rest on the merged runner #102, whose first complete live run is execution `6b297d1d` (2026-09-03). The AIMS/API mutation event is split to #113, blocked by #23. #21 is implemented on `claude/21-recovery-scenarios` (PR pending): the six named events are proven and the reserved `0008` migration is applied, so P2 issue #64 can follow once #21 merges.
- Retry limits, timeout, backoff bounds, and jitter ratio shipped with #20 are **provisional operational defaults**, not measured targets. NFR-004 requires each target to come from a measured baseline and no workload baseline has been captured, so these values must be reviewed against measured latency and failure data before production acceptance.
- Concrete adapter fault mapping is delivered for the source tiers: the shared `adapters/sql_server` helpers map timeout, malformed source data, expired credential, malformed configuration, and unavailability to documented signals, and the #92 fan-out reports each store as a `FailureSignal` rather than driver text. The #24 compatibility reader's mapping was corrected by #110 (PR #111): its schema probe and both label reads now classify by SQLSTATE, so an unreachable, refusing, or password-rejecting AIMS PostgreSQL is `UNAVAILABLE` (retryable) and only an undefined relation, column, or schema is `SCHEMA_DRIFT`. The reader's engines set a connect timeout since #112. The AIMS API mutation mapping belongs with #23. The classification matrix, retry policy, and the #22 ports that carry a `FailureSignal` are complete and independent of them.
- The #22 AIMS ports are synchronous, matching every existing module; the service has no async runtime. The approved foundation plan's Task 5 sketch shows an `async def` page-change adapter. Adapter issues #23 and #24 must either implement synchronously or raise the async decision explicitly before the port signature changes.
- The AIMS label read model is modelled in #22 with only the fields the page-change boundary uses. Its remaining columns, and the vendor's valid page range, are UNKNOWN / NEEDS-DISCOVERY and are deliberately not constrained; supported API documentation is still outstanding.
- The unique active schedule identity per workflow and store is enforced by `0008_authoritative_model_gate` (#21) as a partial unique index on `(workflow_name, store_code) WHERE enabled`, so a disabled schedule stays as history beside its replacement, and `workflow_schedule.configuration_version_id` is NOT NULL. The gate refuses to run over a schedule without a version and names it rather than assigning one; no backfill was needed because the development state store held no schedule.
- #15 records the operator identity and reason on every manual launch but does not evaluate authorization. Role checking is FR-023 and belongs to #26, which depends on #15, #16, and the #28 runtime interfaces.
- The #15 scheduler evaluates cadences but nothing ticks them yet. The timed loop that calls `due_schedules` once a minute is part of the #28 Windows Service and CLI hosting work; until then a schedule launches only when a caller supplies the instant.
- #17 implements the initial no-simultaneous-ownership policy as `no-simultaneous-ownership-v1`, which is deliberately symmetric: neither a scheduled nor a manual request displaces a live owner. FR-017 asks for a defined priority between the two, and no document approves one, so any preference remains UNKNOWN / NEEDS-DISCOVERY pending a business owner. The version is recorded on every decision so a later approved policy is distinguishable in the audit trail.
- A contended launch is rejected rather than queued. Queueing was not chosen because the approved state graph allows only `QUEUED -> RUNNING`, so a queued contender could never be cancelled or expired, and nothing yet starts queued work. If a business owner later approves queueing, both the state graph and a deferred-launch worker are prerequisites.
- The `ScopeContention` guard in the launch path, which rolls back the execution insert when the atomic claim loses a race after the ownership check passed, is defensive and is not covered by an automated test. Reproducing it needs two committed concurrent transactions, which the rollback-based integration fixtures exclude by design. The durable guarantee remains the claim's own conditional predicate.
- Issue #11 was closed after its single-SQL-Server assumption was disproved and was superseded by #91 (shared `DBWH_8555` warehouse), #92 (runtime-discovered per-store sources), #93 (`PEPITO_HO` UOM source), and #94 (`tb_ESL` parity baseline). Its non-goal "Writing to SQL Server" still holds and is reinforced: the replacement writes only to its own PostgreSQL. Issue #38 remains the gate for any parity claim, not for building these read-only adapters.
- The boundary between "read logic to preserve" and "business logic to move into code" is decided as follows: adapters fetch raw rows bounded only by store and source window, and the domain applies every predicate that carries business meaning. Filtering in the adapter would discard rejected rows without a trace, which is the troubleshooting difficulty the replacement exists to remove; filtering in the domain lets each rejection be recorded as a `record_issue` with its reason.
- #78 routes every credential through the DPAPI bundle under four fixed keys, `state.password`, `source.sql.password`, `aims.portal.password`, and `aims.core.password`, and `Settings` has no field that could hold a password. The one pre-existing exception, `ESL_DATABASE_URL` embedding its password, is closed at the startup gate and at engine creation; the model itself stays permissive so fixtures keep working. Every configured target is reported through the existing `HealthService`, with only the state store required, and per-store addresses from `DimStore` are validated as a bare IP or hostname before use. The SQL Server targets have only been probed against synthetic errors, since no SQL Server is reachable from the development machine; their first real exercise waits for the provisioned read-only identity and route. With #78 merged, #11 and #24 lose their credential blocker and retain #38 and the network routes.
- A local clone of `AIMS_PORTAL_DB` (56 tables plus 3 foreign tables) and `AIMS_CORE_DB` (20 tables) now exists, so #24 can be developed and its least-privilege criterion exercised without touching production AIMS. The procedure and the failure modes hit while producing it are tracked by #82; a `SELECT`-only `esl_aims_reader` role mirrors the production identity, and the grant for `AIMS_CORE_DB` is still outstanding.
- #79 delivers `esl-admin secrets set|remove|list` and `esl-admin check-connections`, so a credential can be provisioned into the DPAPI bundle and proven to work without the service running. DPAPI scope is **user scope** (AD-017): the bundle must be written by the Windows Service account, and an administrator resetting that account's password makes it permanently unreadable, which `WORKFLOW.md` now documents with its recovery. Three limits are recorded rather than hidden: the audit entry for a secret change is best-effort, because the state store's own password is provisioned this way and may not be reachable yet; the SQL Server probe is classified by SQLSTATE and unit-tested against synthetic errors only, since no SQL Server is reachable from the development machine; and the ACL writer is proven against the production validator's own reader on Windows, which is also where CI runs. #78 has since consumed it.
- The local AIMS clone procedure is recorded in `docs/development/aims-local-clone.md` (#82), including the eight failure modes met while producing it and the `SELECT`-only `esl_aims_reader` role that mirrors the production identity. The clone is a snapshot with no refresh cadence yet; one becomes necessary once the `page` question is settled and shadow-mode comparison starts.
- #24 delivers the read-only AIMS compatibility reader behind the #22 port, joining Portal (`end_device.station_code`) with Core (`enddevice.pages` JSON `currentPage`) because neither database answers alone. A device with `currentPage = -1` is not returned as a label, since `AimsLabel.page` has no way to say "no page" and `-1` would be misreported; every exclusion, and every malformed or Core-missing row, is counted in the audit entry each read appends, so nothing is dropped silently. The session is opened `default_transaction_read_only=on`, so a write is refused by PostgreSQL before the role's grants are consulted, and the role itself is proven grant-free by test against the local clone. Those integration tests run only where the clone exists, which is the developer machine and not CI; CI runs the pure merge and parsing tests. Still UNKNOWN / NEEDS-DISCOVERY: what `returnPage` and `exceptionPage` mean and which of the three the page-change endpoint sets, so the port keeps modelling one page. The reader is deliberately retirable: swapping it for a vendor-API implementation changes nothing above the port (AD-004).
- #110 corrects the compatibility reader's failure classification, found while scoping #21's AIMS-unavailable scenario. The schema probe had mapped every driver error to `AIMS_COMPATIBILITY / SCHEMA_DRIFT`, which is NON_RETRYABLE, so a stopped, refusing, or password-rejecting AIMS PostgreSQL was recorded as a permanent schema change and never retried; and the two label reads had no mapping at all, so a driver error there reached the runner unclassified and ended the run as OPERATOR_ACTION_REQUIRED. The reader now maps by SQLSTATE and never by message: `42P01`, `42703`, and `3F000` (undefined table, undefined column, invalid schema) stay `SCHEMA_DRIFT`; every other driver error, from the probe or either read, becomes `AimsUnavailable` carrying `AIMS_COMPATIBILITY / UNAVAILABLE`, the RETRYABLE row of the section 8 matrix, with only the relation name in its text. VERIFIED against the local clone for a non-listening port and a rejected login; the stopped-service case was not run because the session was not elevated and is the owner's one-line check recorded in the checkpoint. Still open, deliberately outside #110: the reader's engines set no connect timeout, so a host that drops rather than refuses packets stalls the probe until TCP gives up rather than failing fast into retry.
- #21 delivers the FR-016 recovery scenarios and the reserved `0008_authoritative_model_gate` migration. The gate makes `workflow_schedule.configuration_version_id` NOT NULL behind a preflight that names any schedule without one instead of assigning a version, adds the partial unique index for the active schedule identity, gives `workflow_execution.retry_not_before` so a bounded retry delay survives a process restart (the runner writes it on `RETRY_WAIT`; `runnable_executions(now)` honours it), and gives `execution_step.sequence` a monotonic start order, numbering existing rows by start time then attempt. `application/recovery.py` derives the four recovery fields, the scope, the checkpoint and the step a restart repeats, the actions with unknown external outcome, and the next operator action following architecture section 8, from the execution, its step history, and its unresolved actions; nothing is stored, and presentation is #109. Six scenarios run against committed rows in the dedicated test database through `tests/support`: SQL Server unavailability retried from the checkpoint without repeating discovery; a real socket cut mid-result and a refused connection against the local AIMS clone through a loopback forwarder, both classified `UNAVAILABLE` by the #110 reader and recovered; malformed source data terminal at once with no side effect, a released scope, and a correction as the next action; a runner process killed with `os._exit` after `canonicalize:done` recovered by the next process from the discover checkpoint and persisting exactly once; a process killed after `persist:done` completing with persist resumed under the #104 guard, not repeated; and a first engine vanishing mid-step recovered by a second engine that reuses the scope lease. Recorded limits: SQL Server and malformed-data faults are injected as the signals the adapters raise, because the production sources are never touched and no non-production SQL Server exists; server restart is a simulation, a fresh engine over the same state, not a host reboot; no run step reads the AIMS model, so AIMS-unavailable recovery is proven by chaining the real reader's classification into a runner step; the AIMS/API mutation event is #113 behind #23; the stopped-service case still needs an elevated shell. Two defects found on the way and fixed: `create_execution` read the wall clock instead of the launch instant, and `step_history` ordered by a timestamp this host repeats across a whole run.
- #112 gives the AIMS compatibility reader's two engines a connect timeout, `ESL_AIMS_CONNECT_TIMEOUT_SECONDS` (default 10, bounded 1-300, non-secret, so it appears in the configuration snapshot), passed to psycopg as a connect argument rather than in the URL; a `connect_timeout` already present in a URL keeps precedence, because SQLAlchemy merges connect arguments over URL parameters and the first cut silently turned the #110 tests' two-second URLs into ten-second waits. #110 had found that a closed local port on this host is dropped rather than refused, so psycopg waited past 40 seconds per attempt before `AimsUnavailable` was raised; each retry attempt therefore paid the full handshake timeout before the bounded backoff began. The bound is proven against a released local port both in the unit suite and against the local clone's URL shape, without a `connect_timeout` query parameter. The default matches the 10 seconds the shared SQL Server helpers use and is provisional under NFR-004.
- #114 delivers the snapshot replay: `esl-admin runs replay-snapshot <id> --reason` and `POST /runs/{id}/replay-snapshot` create a `SNAPSHOT_REPLAY` execution linked to its original, refused and audited when the original's finalized `SOURCE_EXPECTED` capture is gone or its latest report is not final (`decide_snapshot_replay`, pure domain). The runner gives it one step, `replay-snapshot` (`application/replay.py`), which copies the retained canonical records verbatim into a new capture under the original's window and versions, proves the aggregate hash reproduces (`SNAPSHOT_REPLAYED` event; a mismatch ends `SUCCEEDED_WITH_EXCEPTIONS`), records hash-decided differences against the store's current expected state, and finalizes a report in which drifted records are `ineligible` and nothing is intended; it never calls a source adapter, proven at the runner and through the real host ports. Because raw source rows are not retained (AD-005), this is reproducibility evidence, not recomputation, and it is named apart from the FR-011 window replay. No migration: `trigger_type` is an unconstrained text column, so the new trigger needs none.
- #64 removes the retention limitation #62 recorded. Three `RESTRICT` links pointed from the retained audit core down into the detailed rows, so a purge could never delete canonical snapshots, the largest class by volume. `0009_relax_action_evidence_links` makes two of them optional, `record_action.record_processing_result_id` and `record_processing_result.canonical_record_snapshot_id`; the third, `reconciliation_exception.record_processing_result_id`, was found during implementation and needed no migration because it was already nullable. `RetentionService.purge_execution` now releases all three deliberately and then deletes record processing results, canonical snapshots, and snapshot sets. `RESTRICT` stays everywhere, so no delete cascades, and nulling costs no audit value: an action keeps its store, item, selling UOM, record key, desired state, and idempotency key, and an exception keeps its category and both evidence sides. Prior-schema coverage proves rows written at `0008` keep their links across the upgrade, and the #62 eligibility guards are unchanged; purge stays disabled by default because no retention duration is approved. The CSV `compatibility_delivery` revision (#30) therefore becomes `0010`.
- #23 delivers `adapters/aims_page.AimsPageClientHttp`, the one path that mutates AIMS, behind the #22 port and under AD-021. Its contract test reads the captured OpenAPI document itself, so a vendor upgrade that changes the operation fails the suite rather than production. Two things the document settled during implementation: it names `402 License is expired` and `405 Parameter is invalid` as documented refusals, both non-retryable; and its `AimsApiResponse` marks **no** field required, so there is no documented vocabulary of success or failure values for `responseCode`. The first cut had invented one, treating an unrecognised code as a rejection; that guess is removed. HTTP status decides, exactly as the document defines it, and the response fields are recorded as receipt evidence, with #24 and #25 reconciliation as the net if the vendor ever answers 200 while declining in the body. It posts the operation of record with the caller's #19 idempotency key in an `Idempotency-Key` header, and its whole design is one distinction: a request that demonstrably never arrived (connection refused, connect timeout, a 502/503/504 gateway) is `NOT_DELIVERED` and retryable, while a request that was written with no usable answer (read timeout, cut connection, 500, or a 200 whose body cannot be parsed) is `UNKNOWN` and reaches an operator instead of being resent (FR-013). Collapsing the second into the first is how a page change silently happens twice. Twenty-two respx contract tests fix every row of the section 8 `AIMS_API` matrix, the request shape against the operation of record, and that no outcome carries the endpoint host, port, or driver text. `ACTIVE` mode stays refused: the adapter is not yet wired into the submission path, because AD-021 requires one controlled live call to settle which of `currentPage`, `returnPage`, `exceptionPage` the endpoint drives before any run may write. New non-secret settings `ESL_AIMS_DASHBOARD_BASE_URL` and `ESL_AIMS_DASHBOARD_TIMEOUT_SECONDS`; no migration; no AIMS database write path, proven by the existing import guard.
- #98 makes provisioning an API token one command. `esl-admin secrets issue-token <account> --reason <ticket> --out <path>` generates 32 random bytes with `secrets.token_urlsafe`, stores them under `api.token.<account>` in the same DPAPI bundle and through the same identity guard as `secrets set`, and reveals the value exactly once through the one channel the caller names, `--out` or `--stdout`; naming both or neither is refused, because a token nobody read is a token nobody can use. Running it again rotates: the command detects that the key existed and says the previous token stops authenticating at the next restart. Four deliberate refusals carry the safety of the command. It will not overwrite an existing `--out` file, which may still hold a token in use; it will not write into a bundle it cannot read; it refuses an account name the bundle key grammar rejects; and it warns, without failing, when the account holds no role in `ESL_OPERATOR_ROLES`, because such a token authenticates and is then refused for every operation. The value reaches no log and no audit entry: the entry is the existing `secret.set` action naming only the key. `scripts/install-service.ps1` gained `-IssueTokensFor` and `-TokenDirectory` so an environment can be stood up in one pass, writing one file per account with the bundle's ACL. Round-trip coverage proves what storage alone cannot: a CLI-issued token authenticates through `tokens_from_bundle` and `BearerTokenAuthenticator` as its own account, rotation invalidates the previous value, and two accounts resolve separately. Not exercised here: the install script's token pass on a staging host, which needs a host this repository does not reach and stays the owner's step.
- #26 delivers the authorized manual-operations service under the two-role model of AD-018: every FR-023 operation, trigger, status, retry, replay, schedule enable/disable, reconciliation, and fallback, passes through `application/operations.py`, which checks the principal's role, insists on a reason for every mutation, and writes a refusal to `audit_entry` under the principal's own name. Three limits are recorded rather than hidden. Roles come from `ESL_OPERATOR_ROLES` and the identity from the running Windows account, because no authenticated session exists until #28; the issue's stated dependency on the #28 runtime interfaces is circular with #28's dependency on #26, and the owner chose to build the authorization layer first. Nothing exposes the service yet: the CLI and web surfaces belong to #28 and #29, so today it is callable only from code and tests. `fallback` automates only the in-application part of the SPECIFICATION section 8 rollback; restoring the legacy SQL Agent schedule, reconciling the window, and opening the incident record remain manual steps the audit entry's `reconcile_window_from` instant is meant to anchor.
- #28 hosts the service: a once-a-minute scheduler tick that launches due schedules through the #15 repository with the cadence-derived window of AD-019, a FastAPI operations API over the #26 service with bearer-token authentication from the bundle, the same operations as `esl-admin runs|schedules|fallback|status|serve` commands, and a Windows Service wrapper whose start, pause, continue, and stop map onto a tested lifecycle that quiesces scheduling first and audits every transition. Four limits are recorded rather than hidden. Nothing executes a launched run: no workflow runner exists until the source adapters #91 to #94 land, so a scheduled or manual execution is created `QUEUED` and stays there, and "checkpoint in-flight work on stop" has nothing to checkpoint yet. The pywin32 wrapper and the composition root (`runtime/host.py`) are exercised by `esl-admin status` and `esl-admin runs list` against the developer environment, not by unit tests; the lifecycle, scheduler, authentication, routes, and CLI they assemble are. Restart recovery of interrupted executions (#18) is not invoked at service start; it belongs with the runner that would resume them. The React operations UI is #29 and consumes this API; the API serves its OpenAPI document only to authenticated callers.
- Discovery of the unknown operational and business contracts listed below.

# Next

1. Retain/export complete Jenkins job definitions and broader history if available; the supplied screenshots/logs are manual evidence snapshots.
2. Retain/export SQL Agent history with calendar dates and root-cause detail if it becomes available; the supplied history is a limited snapshot.
3. Identify the legacy CSV consumer owner and validate the approved automated file/manifest/acknowledgement contract in non-production.
4. Obtain SOLUM-supported API documentation and decide the retirement path for compatibility reads.
5. Confirm promotion precedence, same-economic campaign terms, non-CLR UOM conversion, and final weekday-metadata policy.
6. Measure workload, schedule, latency, failure, and recovery baselines.
7. #114 merged through PR #121 at `2f9eb26`: the source-free snapshot replay, split from AD-016 Task 8 by #21, is complete. #21 merged through PR #117 at `e59c7de`: the six FR-016 events are proven, the `0008` gate is applied, and the four-field recovery report is presented by #109. The AIMS/API mutation event is #113 behind #23; snapshot replay and the traceability matrix are #114 and #116. #115 is implemented on `codex/115-linux-database-verify` with local verification complete; its pull request and Ubuntu database-verify evidence are pending. #37 is merged, but its deployed-parity evaluation still follows Task 8, and deployed compatibility must not be claimed until #38 and representative parity cases are complete.
8. #109 is implemented (Codex, 2026-09-04) and reviewed (Claude, same day; PR pending on the issue-linked branch `claude/109-run-evidence-observability`): the authorized per-run issue, reconciliation, step/recovery, and bounded metrics read surfaces consumed by #29. The independent review found the first cut serving counts by loading whole runs, and on `/metrics` whole windows of runs, with evidence JSONB; the read port now groups, filters, pages, and counts in SQL, the scrape reads counts only, an unknown run and a run without a report are distinct 404s, and a stored secret-like key fails closed with a fixed 500 / CLI exit code 4. The metrics contract is unchanged: the latest 20 runs per workflow/store by default (`ESL_METRICS_RUN_LIMIT`) and aggregate labels without execution IDs. #98 remains the next independent p2 operations issue.
9. #23 is implemented (PR pending): the adapter and its contract tests exist under AD-021, and `ACTIVE` stays refused until one controlled live call settles which page field the endpoint drives. That call, one label in store `075` (two `end_device` rows against 10,832 for `084`), is the owner's and is the last gate before the submission path is wired; #113 follows it. #38 remains blocked on SQL-owner review and still gates any deployed-parity claim. #24 shipped as a read-only compatibility reader against the local AIMS clone.

# Decisions Made

- **AD-001:** The target is a modular single deployable service with clear internal component boundaries and a durable state store.
- **AD-002:** AIMS is a vendor-owned external bounded context. Direct AIMS database writes are prohibited.
- **AD-003:** First cutover may use least-privilege, read-only PostgreSQL compatibility queries encapsulated behind an adapter while supported API alternatives are investigated.
- **AD-004:** No repository-local project skill is created yet. The official documentation search did not establish a repository-local discovery convention that can be verified for this environment; there is no repeatable project-specific workflow requiring one at this phase.
- **AD-005:** Canonical records are compared in memory; complete immutable canonical snapshots under configurable retention, hashes/differences, checkpoints, and delivery state are persisted in the target service database. Physical CSV files are not target workflow state, comparison evidence, or audit; they remain only as a temporary compatibility delivery adapter if an external consumer requires them.
- **AD-006:** Use Python + PostgreSQL as an internal web application with browser UI/API and CLI. It can run continuously as a dedicated Windows Service or directly by command line for development, diagnostics, and controlled administration. PostgreSQL is the target state/audit/queryable-log database.
- **AD-007:** The available high-spec Windows 10 PC is accepted as the production host under explicit operational risk acceptance. Builds/releases use GitHub Actions to create approved immutable artifacts, which are currently transferred through a controlled method and installed locally. GitHub access may be enabled later after security review and outbound HTTPS allowlisting; secrets remain Windows DPAPI-protected, ACL-restricted per-environment bundles outside the repository.
- **AD-008:** Use FastAPI for the internal backend/API and React + TypeScript + Vite + Tailwind CSS for the browser UI. Google Stitch exports are versioned design handoffs, not production application code; React uses authenticated FastAPI endpoints only.
- **AD-013:** `develop` is the remote integration branch. Issue branches start from it, target it through pull requests, use GitHub auto-merge only after required checks/review, and local `develop` is fast-forwarded after each merge.
- **AD-012:** Use an automated ACL-restricted CSV/ready-manifest/acknowledgement handshake for bounded compatibility delivery. PostgreSQL remains authoritative for lifecycle/audit; filesystem presence is not completion, no blind resend is permitted, and the adapter stays disabled until consumer acceptance.
- **AD-014:** Every implementation is traced to one GitHub issue. A meaningful issue/epic branch starts from current `develop`, is committed and pushed after verification, merges back to `develop`, and is followed by a local fast-forward pull of remote `develop`.
- **AD-015:** Promotion behavior is extracted into independently testable domain rules. The initial target follows the supplied reference-directed policy, records ambiguity, and does not invent a campaign winner, generic member filter, non-CLR UOM conversion, or manual-text parsing rule; deployed legacy parity is separately measured and not assumed.
- **AD-016:** Use a hybrid relational plus versioned-JSONB application data model. Stable identities/states/relationships/query fields are relational; complete immutable canonical snapshots and evolving evidence are typed, versioned, hashed JSONB under configurable retention. Business/model documentation is reviewed before app/schema changes.
- **AD-017:** The runtime secret bundle is DPAPI-encrypted under user scope and written only by the service account through `esl-admin secrets`; machine scope was rejected because it lets any process on the host decrypt.
- **AD-018:** Manual operations are authorized under a two-role model, `operator` and `admin`, versioned `two-role-v1`. Identity is supplied by the caller and roles by `ESL_OPERATOR_ROLES` until #28 authenticates; a principal without a role is refused and every refusal is audited. `fallback` is the in-application part of the cutover rollback and never touches executions or audit rows.
- **AD-019:** The internal operations API authenticates with per-account bearer tokens held in the DPAPI bundle under `api.token.<account>`, resolved to the same principal and roles as the CLI. A scheduled run's source window runs from the previous instant on its own cadence to the launch instant. The scheduler and API are hosted as one Windows Service whose pause and stop quiesce scheduling first.
- **AD-020:** Every SQL Server source tier is read under a configured isolation level, `READ COMMITTED` by default and `SNAPSHOT` only by setting, recorded in each read's provenance. Decided after a read-only catalog check found snapshot isolation OFF on all three source databases; the `SNAPSHOT`-only engine of #91 could read nothing.
- **AD-021:** The AIMS mutation boundary is the deployed Dashboard operation `POST /dashboardservice/common/labels/page?store=<code>` carrying a `pageChangeList` of `labelCode`/`page` entries. **Corrected twice on 2026-09-04 while implementing #23.** First, the original wording cited `docs/hop-jenkins-pipeline/AIMS-Dashboard-Diagnostic-20260807-171837.txt`, which documents the *article* endpoint, not this one. Then the owner captured the deployed document itself to `docs/hop-jenkins-pipeline/AIMS-Dashboard-OpenAPI-common.json`, so the contract of record is now a file in the repository rather than a transcription, and the #23 contract test reads it. It confirms every transcribed detail exactly: `operationId` `changeDisplayPage`, a required `store` query parameter, a required body of `UpdateDisplayPage` carrying `pageChangeList` of `labelCode` and integer `page`, and an `AimsApiResponse` of `responseCode`, `responseMessage`, and `customBatchId`. It also settles the security question more firmly than the earlier reading: `security` is an explicit empty array at the document root and the components define no `securitySchemes` at all. No separate written vendor confirmation is awaited. The same diagnostic records `Operation security`, `Global security`, and `securitySchemes` all null, so the absence of an authentication scheme is a property of the vendor's endpoint, not a document in transit; and the Apache Hop and Jenkins pipeline this project replaces exists precisely because the Dashboard does not meet the backend requirement, so waiting for vendor endorsement would make the replacement unreachable by construction. The unauthenticated HTTP page-change call is accepted as an explicit operational risk on the footing of AD-007, and is not worsened here: the replacement sends the request the current pipeline already sends, but behind the idempotency keys of #19, the failure classification and bounded retry of #20, the action ledger and reconciliation of #25, the never-auto-resend rule for an unknown outcome (FR-013), and SHADOW mode by default with ACTIVE refused until an owner enables it. Which of `currentPage`, `returnPage`, and `exceptionPage` the endpoint drives stays UNKNOWN and is settled by one controlled live call, never by assumption. TLS and authentication are adopted the moment the vendor supports them; direct writes to any AIMS database remain forbidden (AD-002).

# Risks / Blockers

- **Medium:** Jenkins configuration screenshots and SQL Agent history are limited manual snapshots; complete Jenkins XML/retry settings and calendar-dated SQL Agent root-cause history remain unavailable.
- **High:** Promotion selection is not deterministic for overlapping campaigns; time/day and priority semantics require business confirmation.
- **High:** The target CSV contract is approved, but the legacy consumer owner has not accepted its schema, service identities/ACLs, timeout, retention, or rollback; production delivery therefore remains disabled.
- **Medium:** Current AIMS reads rely on database structures that are not vendor-supported contracts.
- **Accepted under AD-021:** The AIMS page-change endpoint declares no authentication scheme and the supplied artifact shows HTTP; the deployed Dashboard's own OpenAPI records `Operation security`, `Global security`, and `securitySchemes` all null. That is a property of the vendor's endpoint rather than a pending document, and the current production pipeline already posts to it unauthenticated, so the risk is accepted on the footing of AD-007 and revisited the moment SOLUM supports authentication and TLS.
- **Medium:** No current workload, timing, availability, RPO/RTO, or recovery baseline was supplied.
- **High:** The latest supplied `RefreshESL_New` is marked review/test only and directly invokes itself. This indicates apparent unbounded recursion if executed; its safe deployment and production parity are unverified and require #38 DBA/SQL-owner review.

# Discoveries

- **VERIFIED:** `dbo.RefreshESL_New` processes stores `075` and `084`, builds product/promotion state, and transactionally deletes, updates, and inserts `ESL.dbo.tb_ESL` rows.
- **VERIFIED:** The procedure reads **three source tiers**, not one SQL Server. `DBWH_8555` is reached by three-part name, so it is another database on the same instance as `ESL`, and supplies `DimStore`, `DimItemMapping`, and `FactCampaign`. `PEPITO_HO`, the central iRetail database, is reached by four-part name through a linked server and supplies `ITEM_UOM_MAPPING_MST`. Each store runs a local iRetail SQL Server reached by four-part name through a per-store linked server, supplying twelve campaign, item, stock, and movement tables. Sixteen source objects are live in total.
- **VERIFIED:** Per-store connection targets are **data-driven, not configured**. The procedure reads `ORG_IP` and `ORG_DB` from `DBWH_8555.dbo.DimStore` for each store code and builds four-part names by string concatenation, so the set of store servers is discovered at runtime. `ORG_IP` holds a bare IP address with no port. This is consistent with FR-026: adding a store is data, not code.
- **VERIFIED:** Three referenced objects are **dead code**, sitting inside commented-out blocks and never executed: the `STORE_OPS_APP` linked server, the `benchmark_master_selected_items` table it fronts, and `DimItem`. All three appear only in the disabled `REDLIST` update, so none is a dependency of the replacement.
- **VERIFIED:** Because that block is disabled, `tb_ESL.REDLIST` is never populated and retains the empty string it is initialised with. Whether the field is permanently retired or temporarily switched off is **UNKNOWN / NEEDS-DISCOVERY**; the replacement replicates the current behaviour and invents nothing.
- **VERIFIED:** The AIMS label read model lives in `AIMS_PORTAL_DB.end_device_templates (label_code, name, page, station_code)`, keyed `(label_code, page)`. Its `station_code` is **not** a store code: all 10,834 rows hold the literal `DEFAULT_STATION_CODE`. The real store code is on `end_device` and `end_device_articles`, so reading labels for a store requires joining `end_device` to `end_device_templates` rather than filtering the template table.
- **VERIFIED:** `AIMS_PORTAL_DB` reaches into `AIMS_CORE_DB` through `postgres_fdw`, using the server `fdw_aims_core_db`, an `aims` user mapping, and the foreign tables `enddevice`, `slabel`, and `station`, with a materialized view `enddevice_info` built on them. Portal therefore holds both `end_device`, its own table, and `enddevice`, a foreign table from Core; the two are one underscore apart and carry different data. The replacement reads both databases directly rather than depending on the vendor's internal FDW configuration.
- **VERIFIED:** Store `075` is effectively not fitted with labels. AIMS holds 15,010 `article` rows for it but only 2 `end_device` rows, against 10,832 for store `084`. This matches the existing evidence that the Hop pipeline constrains its SQL to `084`: store `075` is computed by SQL Server but never reaches AIMS. Confirmed intentional by the source owner, so a parity comparison must expect `075` to be absent on the AIMS side rather than treat it as a defect.
- **VERIFIED:** One AIMS row carries a malformed store code. `article_id = 101066052139` has `station_code = '"075'`, four characters with a leading double quote, which silently escapes an exact-match filter on `075`. It is a real production data defect and a useful fixture for the structural validation built in #12.
- **VERIFIED:** The displayed page is held in `AIMS_CORE_DB.enddevice.pages`, not in Portal. It is JSON inside a `varchar(100)`, shaped `{"currentPage":N,"returnPage":N,"exceptionPage":N}`. `Portal.end_device_templates.page` is a template slot and is `0` on every row, so it is not the displayed page. Reading a label's page therefore requires both databases: the store code exists only in Portal and the current page only in Core.
- **VERIFIED:** Current page values are `1` on 10,518 devices, `-1` on 4,629, `2` on 333, `4` on 28, and `3` on exactly one. The single page-3 device matches the documented behaviour of the Hop pipeline `esl-sku-promo-multi-page.hpl`, so multi-page is genuinely used but rare. `-1` correlates with `state = UNASSIGNED` (4,629 against 4,675) and is a sentinel for a device with no assignment, not a page number; treating it as one would misreport unassigned devices.
- **UNKNOWN / NEEDS-DISCOVERY:** The meaning of `returnPage` and `exceptionPage`, and which of the three the AIMS `POST /common/labels/page` endpoint actually sets. The #22 port models a single page, which is sufficient only if the endpoint drives `currentPage` alone.
- **VERIFIED:** Business rules are embedded inside the procedure's SQL predicates rather than expressed separately, for example campaign status, campaign type membership, validity dates, and the PFS exclusion. That entanglement is the stated reason the current pipeline is difficult to troubleshoot.
- **VERIFIED:** SQL Agent job `Refresh ESL Data` executes `RefreshESL_New` every 30 minutes from 07:00 to 23:59 daily, with zero retries.
- **VERIFIED:** `RefreshESL_New` currently enumerates two stores (`075` and `084`); target requirements now require a configurable multi-store workflow that can extend beyond them.
- **VERIFIED:** For scalable `KGS` items, the source price is per kilogram and ESL display price is per 100 grams (`50,000/kg` → `5,000/100GR`).
- **VERIFIED:** The supplied Hop masters constrain their SQL to store `084` and invoke SKU and promotion branches.
- **VERIFIED:** SKU processing compares `tb_ESL` with AIMS Portal article data and creates a CSV of new/changed SKU data.
- **VERIFIED:** The newest `backup-pipeline/esl-sku-promo-multi-page.hpl` enables Page 3 REST and uses `STORE_CODE_PARAM`; the older `backup pipeline` snapshot is historical evidence with different behavior.
- **VERIFIED:** Hop uses SQL Server, AIMS Portal PostgreSQL, local files, and an AIMS Dashboard page-change HTTP endpoint. Credentials/configuration references exist in evidence but are intentionally not reproduced here.
- **VERIFIED:** The live AIMS Dashboard OpenAPI documents `POST /common/labels/page` for page changes, but declares no API security scheme. It accepts a store query parameter and a `pageChangeList` of label/page pairs and returns response code/message plus batch ID.
- **VERIFIED:** CSV samples provide evidence of Page 1/Page 2/Page 3/Page 4 operational outputs but do not identify a SKU CSV consumer or acknowledgement contract.
- **VERIFIED:** The legacy SKU output definition contains 42 ordered fields, UTF-8 encoding, comma delimiter, DOS line endings, and no header. The approved target contract adds automatic atomic publication and matching acknowledgement without treating files as durable state.
- **VERIFIED:** The newly supplied promotion reference directs target policy for primary date/time eligibility, category-`001` regular price, explicit PFS exclusion, structured promotion validation, CLR normalization, no invented non-CLR conversion, post-calculation KGS display transformation, raw `DISC_TEXT`, atomic promotion state, and store/item/selling-UOM isolation. It is not by itself deployed-parity evidence.
- **VERIFIED:** The same reference requires observable ambiguity for different calculated economic outcomes and same-economic/different-term outcomes, while formal priority and effective-price/rounding comparison remain unresolved.
- **VERIFIED:** The latest supplied procedure source is review/test only, directly invokes itself, filters campaign status, and does not establish cross-midnight or calculated-economic parity; #38 captures the SQL-review gate.
- **VERIFIED:** On the development host `datetime.now()` returned only 2 distinct values in 2,000 consecutive calls (Python 3.12 on Windows 11), so every execution and step created within one run carried the same `started_at`, and ordering by it fell to physical order, which an UPDATE changes in PostgreSQL. `0008` therefore gives `execution_step` a monotonic `sequence`, as 0007 did for events, and a launch stamps `started_at` from the launch instant it already holds.
- **VERIFIED:** Three tables reference `record_processing_result` under `RESTRICT`: `record_action`, `record_issue`, and `reconciliation_exception`. Architecture 5.8 and issue #64 named only the first, because the #62 analysis traced the action path; `record_issue` is deleted by the purge before the result, and `reconciliation_exception` is retained audit core whose link was already nullable. A purge must therefore release three links, not two.
- **INFERRED:** The supplied artifacts are a snapshot/backup and are not a complete representation of production.

# Verification Performed

- Enumerated the repository files and inspected the SQL procedure, table DDL, troubleshooting report, Hop project metadata, all `.hpl`/`.hwf` definitions, database metadata, and the supplied current-state runbook.
- Confirmed required documentation exists and contains no copied credentials or secret values.
