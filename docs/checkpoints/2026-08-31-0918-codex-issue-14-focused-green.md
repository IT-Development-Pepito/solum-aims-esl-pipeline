- **Timestamp / owner:** 2026-08-31 09:18 +08:00; Codex issue #14 focused green checkpoint.
- **Issue:** GitHub #14, `[workflow] model explicit workflow dependencies and terminal states`, assigned to `it20pepito`; labels `type:feature`, `area:workflow`, and `priority:p1`.
- **Git state:** `codex/14-explicit-workflow-states` in `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-14-explicit-workflow-states`, based on `origin/develop` at `02f05f3bdeeb0bf6224567c382b10e2bb2e5fdf5`; changes remain uncommitted; no PR yet.
- **Scope:** Added FR-007 tests and the minimal pure-domain workflow contract: approved execution transitions/terminal states, structured accepted/rejected transition evidence, explicit terminal predecessor conditions, deterministic dependency-first ordering, and rejection of unknown/cyclic dependency graphs. Modified `src/esl_service/domain/__init__.py`; added `src/esl_service/domain/workflow.py` and `tests/unit/domain/test_workflow.py`. Non-goals remain unchanged; there is no persistence or migration change.
- **Evidence:** Red: `python -m pytest tests/unit/domain/test_workflow.py -v` produced 12 expected failures because `esl_service.domain.workflow` was absent. Green: Ruff passed for the changed domain/test files; mypy passed for 5 domain source files; `python -m pytest tests/unit/domain -q` reported 26 passed; `git diff --check` passed. Python 3.12.7.
- **Configuration:** No configuration value or secret was read or changed. The root `.venv` is reused with `PYTHONPATH` directed to this worktree's `src` directory.
- **External state:** None. No PostgreSQL, SQL Server, AIMS, Jenkins, Hop, CSV delivery, ESL device, or production system was contacted or mutated.
- **Risks / next action:** Persistence remains deliberately deferred to #18. Next action: update the high-level progress summary, run the full required Python/frontend verification gate, then request an independent code review before commit/PR.
