- **Timestamp / owner:** 2026-08-31 11:05 +08:00; Claude issue #18 implementation.
- **Issue:** GitHub #18, [workflow] persist checkpoints and recover interrupted executions, assigned to it20pepito; labels type:feature, area:persistence, area:workflow, and priority:p1. Parent epic #6. Dependency #14 is merged at 228746e.
- **Git state:** claude/18-restart-safe-execution-state in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-18-restart-safe-execution-state, branched from origin/develop at 228746e597ec510e9f31bb727ab790009f618775.
- **Scope:** AD-016 Task 3 for FR-010, with FR-002, FR-009, and FR-025 support. Added `domain/outcomes.py` (`ExecutionMode`, `TriggerType`, `FailureClass`, `NewExecution`) which consumes the reviewed #14 workflow contract and redefines no state or transition. Extended `workflow_execution` with the complete input scope, `ended_at`, and `terminal_reason`; extended `scope_lease` with `heartbeat_at`, `expires_at`, `released_at`, and `lease_version`; added `execution_step` and `execution_checkpoint`; replaced the CASCADE foreign keys on `scope_lease`, `execution_event`, and `record_action` with RESTRICT. Extended `ExecutionRepository` with `create_execution(NewExecution)`, `transition_execution` (compare-and-set plus atomic audit event), `claim_scope`/`heartbeat_scope`/`release_scope`/`expired_leases`/`get_lease`, `start_step`, `finish_step`, `append_checkpoint`, and `recoverable_executions`. Added migration `0003_execution_recovery`. Migrated every `create_execution` call site to `NewExecution` with no positional overload retained, and moved the shared execution factory into `tests/factories.py` with a `configuration_version_id` fixture in `tests/integration/conftest.py`. Non-goals confirmed untouched: no per-record outcome table (#12), no action lifecycle or `OUTCOME_UNKNOWN` (#19), no reconciliation (#25), no AIMS transport, no scheduler, no UI/API; `0001_operational_state.py` and `0002_configuration_and_snapshots.py` are unchanged.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. RED was a real failure, not a skip: `ModuleNotFoundError: No module named 'esl_service.domain.outcomes'`. GREEN: 16 new recovery tests pass; full suite 101 passed with the dedicated database and 74 passed / 27 skipped without one, matching how CI runs. Migration round trip verified: upgrade `0003`, downgrade to `0002_configuration_and_snapshots`, upgrade to head; `alembic current` reports `0003_execution_recovery (head)`, so the graph stays single-headed and the #49 guard passes. Ruff passed; mypy passed for 18 source files; frontend typecheck, Vitest 1 passed, and Vite build passed; `git diff --cached --check` clean.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; the value was never printed, logged, or committed. `ESL_DATABASE_URL` was set from it for Alembic invocation only. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Applied and rolled back migration `0003` on the dedicated non-production database `esl_pipeline_test` on localhost, which now rests at `0003`; integration tests roll back their own rows. Commented on GitHub issue #18. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** Two of #18's acceptance criteria are only partially met by design and are recorded on the issue: per-record outcome belongs to #12 and unknown-submission reconciliation to #19 and #25. `0003` adds NOT NULL columns and asserts `workflow_execution` is empty rather than inventing values; that assertion holds today in every environment because the service has never run, and it fails loudly instead of guessing if it ever does not. A rejected transition raises without persisting its rejection event, because an exception inside a caller's transaction cannot guarantee durable evidence; the exception carries the audit event so a caller can record it on a transaction it controls. `domain/__init__.py` was deliberately not extended with the new outcome types, so imports use `esl_service.domain.outcomes` directly. Next action: open the PR to `develop`, link #18 in the Development sidebar because the closing keyword is inert on a `develop`-targeted PR, and record the cross-agent review.
