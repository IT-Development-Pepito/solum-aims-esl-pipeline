- **Timestamp / owner:** 2026-08-31 12:40 +08:00; Claude issue #36 implementation.
- **Issue:** GitHub #36, [domain] model promotion eligibility, UOM, and atomic state, assigned to it20pepito; labels type:feature, area:domain, area:ingestion, and priority:p1. Parent epic #5. Branch created with `gh issue develop`, so it is linked in the issue's Development panel.
- **Git state:** claude/36-promotion-evidence in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-36-promotion-evidence, branched from origin/develop at 3317483.
- **Scope:** AD-016 Task 4 plus the reference-directed rules the issue names, for FR-003, FR-004, FR-005, FR-006, FR-026, FR-027 and BR-011 through BR-018. Added `domain/promotion_evidence.py` (PromotionType, PromotionOutcome, CandidateEligibility, WeekdayEvidence, reason/fallback codes, PromotionCandidateEvidence, PromotionEvaluationEvidence with atomic-state, key, uniqueness and selection invariants) and `domain/promotion_rules.py` (cross-midnight date/time eligibility, explicit PFS exclusion, category-001 regular price, structured value validation, CLR normalization with non-CLR anomaly, post-economic KGS to /100GR display, raw DISC_TEXT preservation, `evaluate_candidate`, `evaluate`, `build_state`). Added `promotion_evaluation` and `promotion_candidate_snapshot`, `PromotionEvidenceRepository.record_evaluation`/`get_evaluation`, and migration `0004_promotion_evidence`. Non-goals honored: no winner-selection function anywhere, no non-CLR conversion, no DISC_TEXT parsing, no rounding policy, no ambiguity classification into BR-019 priority codes, no SQL Server or AIMS access, no claim of deployed parity.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. Two red/green cycles, both real failures rather than skips: `ModuleNotFoundError: No module named 'esl_service.domain.promotion_evidence'` then `ModuleNotFoundError: No module named 'esl_service.persistence.evidence_repository'`. Domain green at 73 tests; persistence green at 9. Full suite 153 passed with the dedicated database and 117 passed / 36 skipped without one, matching CI. Migration round trip verified: upgrade `0004`, downgrade to `0003_execution_recovery`, upgrade to head; `alembic current` reports `0004_promotion_evidence (head)`, single-headed so the #49 guard passes. Ruff passed; mypy passed for 21 source files; frontend typecheck, Vitest 1 passed, Vite build passed; `git diff --cached --check` clean.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. `ESL_DATABASE_URL` set from it for Alembic only. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Applied and rolled back migration `0004` on the dedicated non-production database `esl_pipeline_test` on localhost, which now rests at `0004`; integration tests roll back their own rows. Created the linked branch on GitHub. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** Two deliberate deviations are recorded in the PR. First, the `promotion_evaluation` check that a SELECTED outcome names a candidate had to be narrowed to the enforceable direction, "a linked candidate implies SELECTED", because the candidate foreign key is circular and PostgreSQL cannot defer a CHECK; the converse is enforced by the domain invariant. Second, `evaluate` treats an unresolved candidate as outranking a rejected one when none is eligible, so unresolved work blocks rather than being dropped. Window bounds are inclusive, matching source BETWEEN semantics; legacy boundary parity is explicitly not claimed and remains the #38 gate. Ambiguity is recorded without classifying into PROMO_PRIORITY_DIFFERENT_ECONOMIC or DISPLAY_PRIORITY_SAME_ECONOMIC, by decision, because effective-price comparison and rounding remain UNKNOWN / NEEDS-DISCOVERY; #37 consumes this evidence. Next action: open the PR to `develop` and record the cross-agent review.
