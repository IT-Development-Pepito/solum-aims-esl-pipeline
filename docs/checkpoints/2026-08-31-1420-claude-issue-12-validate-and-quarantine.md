- **Timestamp / owner:** 2026-08-31 14:20 +08:00; Claude issue #12 implementation.
- **Issue:** GitHub #12, [domain] validate source records and quarantine safe rejections, assigned to it20pepito; labels type:feature, area:domain, area:ingestion, and priority:p1. Parent epic #5. Branch created with `gh issue develop`, so it is linked in the issue's Development panel.
- **Git state:** claude/12-validate-and-quarantine in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-12-validate-and-quarantine, branched from origin/develop at b3ae64e.
- **Scope:** AD-016 Task 5 plus the structural validation the issue names, for FR-003, FR-006 and BR-006, BR-011 through BR-017, BR-019. Added `domain/validation.py` (`validate_batch` for required reproducibility fields and canonical-key uniqueness, `promotion_issues` surfacing #36 reason and fallback codes as traceable record issues, `assess_record` combining both into one outcome). Extended `domain/outcomes.py` with ValidationStatus, EligibilityStatus, ActionDecision, ProcessingStatus, RecordIssueEvidence and RecordProcessingEvidence, and `domain/serialization.py` with `sanitize_evidence`. Added `persistence/models/outcomes.py`, `RecordOutcomeRepository`, and migration `0005_record_outcomes`. Non-goals honored: no automatic correction of rejected data, no automatic selection of an ambiguous campaign, no external mutation, no invented range or domain thresholds.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. Two red/green cycles, both real failures rather than skips: `ImportError: cannot import name 'ActionDecision'` then `ImportError: cannot import name 'RecordOutcomeRepository'`. Domain green at 108 tests, persistence green at 7. Full suite 195 passed with the dedicated database and 152 passed / 43 skipped without one, matching CI. Migration round trip verified: upgrade `0005`, downgrade to `0004_promotion_evidence`, upgrade to head; `alembic current` reports `0005_record_outcomes (head)`, single-headed so the #49 guard passes. Ruff passed; mypy passed for 23 source files; frontend typecheck, Vitest 1 passed, Vite build passed; `git diff --cached --check` clean.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. `ESL_DATABASE_URL` set from it for Alembic only. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Applied and rolled back migration `0005` on the dedicated non-production database `esl_pipeline_test` on localhost, which now rests at `0005`; integration tests roll back their own rows. Created GitHub issue #58 recording the undocumented validation thresholds, and created the linked branch. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** Validation is deliberately structural only. FR-003 also requires "configured range/domain rules", but no price, stock, quantity, weight, expiry, UOM-domain, or percentage thresholds are documented anywhere in `docs/`; inventing them would breach the AGENTS.md rule against inventing business rules, so issue #58 now tracks that discovery gap and this issue stops short of it. One defect was found and fixed during implementation: several issues for one record shared an `occurred_at`, so ordering fell back to a random UUID and the order the rules produced them was lost; `record_issue` now carries an explicit `sequence` ordinal, unique per result. Severity and classification are stored as bounded free text rather than enums, because no vocabulary for them is documented. Next action: open the PR to `develop` and record the cross-agent review.
