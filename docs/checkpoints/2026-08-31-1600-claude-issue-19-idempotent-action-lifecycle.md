- **Timestamp / owner:** 2026-08-31 16:00 +08:00; Claude issue #19 implementation.
- **Issue:** GitHub #19, [workflow] enforce logical idempotency keys for retries and restarts, assigned to it20pepito; labels type:feature, area:persistence, area:workflow, and priority:p1. Parent epic #6. Branch created with `gh issue develop`, so it is linked in the issue's Development panel.
- **Git state:** claude/19-idempotent-action-lifecycle in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-19-idempotent-action-lifecycle, branched from origin/develop at 10eb6db.
- **Scope:** AD-016 Task 6 for FR-013, implementing architecture section 5.6 exactly. Added `domain/actions.py` (ActionState, DeliveryCertainty, ActionTransitionEvent, InvalidActionTransition, NewRecordAction, ActionAttemptEvidence, `build_idempotency_key`, `is_terminal_action`, `requires_reconciliation`, `transition_action`), `persistence/models/actions.py` (ActionAttempt), and `persistence/action_repository.py` (create_intended, transition, append_attempt, unresolved_actions). Extended `record_action` into the durable logical ledger and added migration `0006_action_lifecycle`. `ExecutionRepository.record_action` now forwards to ActionRepository for one release, as the plan directs. Non-goals honored: no blind retry of an ambiguous external action, no AIMS transport, no scheduler, no reconciliation report.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. Two red/green cycles, both real failures rather than skips: `ModuleNotFoundError: No module named 'esl_service.domain.actions'` then `ModuleNotFoundError: No module named 'esl_service.persistence.action_repository'`. Domain green at 33 tests, persistence green at 12. Full suite 240 passed with the dedicated database and 185 passed / 55 skipped without one, matching CI. Migration round trip verified: upgrade `0006`, downgrade to `0005_record_outcomes`, upgrade to head; `alembic current` reports `0006_action_lifecycle (head)`, single-headed so the #49 guard passes. Ruff passed; mypy passed for 26 source files; frontend typecheck, Vitest 1 passed, Vite build passed; `git diff --cached --check` clean.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. `ESL_DATABASE_URL` set from it for Alembic only. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Applied and rolled back migration `0006` on the dedicated non-production database `esl_pipeline_test` on localhost, which now rests at `0006`; integration tests roll back their own rows. Created the linked branch. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** OUTCOME_UNKNOWN deliberately has no automatic path back to SUBMITTING, so an ambiguous submission blocks until an operator reconciles it; that is the FR-013 intent but it means unresolved actions accumulate until #25 provides the reconciliation surface. `build_idempotency_key` hashes a typed private dataclass rather than the plain dictionary the plan sketched, because the canonical serializer deliberately refuses untyped mappings; the contributing fields are identical and each is covered by a test proving it changes the key. `DeliveryCertainty` values CONFIRMED, NOT_DELIVERED, and UNKNOWN are a minimal vocabulary: the architecture names the concept but not its values. `0006` adds NOT NULL columns and asserts `record_action` is empty rather than inventing an idempotency key it cannot reconstruct. Next action: open the PR to `develop` and record the cross-agent review.
