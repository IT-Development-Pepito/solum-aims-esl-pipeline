- **Timestamp / owner:** 2026-08-31 17:45 +08:00; Claude issue #25 implementation.
- **Issue:** GitHub #25, [operations] reconcile workflow outcomes and complete audit ledger, assigned to it20pepito; labels type:feature, area:operations, area:persistence, and priority:p1. Parent epic #7. Branch created with `gh issue develop`, so it is linked in the issue's Development panel.
- **Git state:** claude/25-audit-and-reconciliation in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-25-audit-and-reconciliation, branched from origin/develop at 19d1641.
- **Scope:** The #25 half of AD-016 Task 7, which the plan splits as "#25 owns persistence/query; #27 owns retention configuration and read schemas", for FR-021 and FR-022. Added `domain/reconciliation.py` (ReconciliationMode, ReconciliationCounts, UnbalancedReconciliation, `validate_balance` implementing the architecture 5.7 formulas exactly), `persistence/models/reconciliation.py` (AuditEntry, ReconciliationReport, ReconciliationException), `persistence/reconciliation_repository.py` (finalize_report, list_exceptions, append_audit_entry, query_audit, query_events), and migration `0007_audit_reconciliation`. Deliberately not in scope and left to #27: `persistence/retention.py`, the retention settings in `config.py`, and `web/audit_schemas.py` / `web/audit_queries.py`.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. Two red/green cycles, both real failures rather than skips: a collection error for the missing `esl_service.domain.reconciliation`, then `ModuleNotFoundError: No module named 'esl_service.persistence.reconciliation_repository'`. Domain green at 10 tests, persistence green at 13. Full suite 263 passed with the dedicated database and 195 passed / 68 skipped without one, matching CI. Migration round trip verified in both directions after the correction described below; `alembic current` reports `0007_audit_reconciliation (head)`, single-headed so the #49 guard passes. Ruff passed; mypy passed for 29 source files; frontend typecheck, Vitest 1 passed, Vite build passed; `git diff --cached --check` clean.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. `ESL_DATABASE_URL` set from it for Alembic only. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Applied and rolled back migration `0007` on the dedicated non-production database `esl_pipeline_test` on localhost, which now rests at `0007`. During implementation the migration was edited while the database already carried its earlier version, so its newer downgrade referenced a constraint that version had not created; the three tables were dropped, the database was stamped back to `0006_action_lifecycle`, and the corrected `0007` was reapplied and then round-tripped cleanly. Only the dedicated test database was affected. Created the linked branch. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** A defect was found and fixed: audit entries written in one transaction share `occurred_at`, so an audit trail's order fell back to a random UUID. `audit_entry` now carries a monotonic identity ordinal, and `execution_event` gained the same column because `query_events` is an interface this issue ships and had the identical latent defect; both queries order by it. Exception categories are taken from the durable issue codes recorded by #12 rather than a parallel vocabulary, so `UOM_RULE_REQUIRED`, `INVALID_PERCENT_VALUE`, and `MISSING_WEEKDAY_METADATA` enumerate today. The two BR-019 sub-classifications, PROMO_PRIORITY_DIFFERENT_ECONOMIC and DISPLAY_PRIORITY_SAME_ECONOMIC, are representable but not yet produced, because classifying ambiguity was deliberately left to #37; when #37 emits them they flow through unchanged. Next action: open the PR to `develop` and record the cross-agent review; #27 completes Task 7 with retention and read schemas.
