- **Timestamp / owner:** 2026-08-31 19:00 +08:00; Claude issue #27 implementation.
- **Issue:** GitHub #27, [operations] expose health and validate versioned configuration, assigned to it20pepito; labels type:feature, area:operations, area:runtime, and priority:p1. Parent epic #7. Branch created with `gh issue develop`, so it is linked in the issue's Development panel.
- **Git state:** claude/27-health-and-configuration in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-27-health-and-configuration, branched from origin/develop at 422a072.
- **Scope:** Implemented issue #27 as written for FR-024 and FR-025, after confirming a scope conflict with the AD-016 plan. The plan's Task 7 assigns #27 retention and audit read schemas, but issue #27 describes liveness, readiness, dependency health, and versioned configuration validation, which are largely disjoint deliverables. The issue as written was chosen; the plan's orphaned retention and read-model work was raised as new issue #62 so it is not lost. Added `runtime/health.py` (HealthState, DependencyHealth, DependencyProbe, HealthReport, HealthService), `persistence/health.py` (StateStoreProbe), and extended `config.py` with CONFIGURATION_SCHEMA_VERSION, ConfigurationProblem, `describe_configuration_problems`, `validate_startup_configuration`, `sanitized_configuration_snapshot`, and `configuration_content_hash`. No migration: this issue adds no persistence. FastAPI routes remain with #26, #28, and #29.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. RED was a real failure, not a skip: `ImportError: cannot import name 'CONFIGURATION_SCHEMA_VERSION' from 'esl_service.config'`. Unit green at 25 tests, state-store integration green at 5. Full suite 293 passed with the dedicated database and 221 passed / 72 skipped without one, matching CI. Ruff passed; mypy passed for 31 source files; frontend typecheck, Vitest 1 passed, Vite build passed; `git diff --cached --check` clean. Migration set is unchanged, so the graph stays single-headed at `0007_audit_reconciliation` and the #49 guard passes.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. No production configuration, secret bundle, or DPAPI material was read. `SECRET_BEARING_SETTINGS` excludes `database_url` and `secret_bundle_path` from every configuration snapshot.
- **External state:** Read-only connectivity checks against the dedicated non-production database `esl_pipeline_test` on localhost, plus one deliberate failed connection to 127.0.0.1:1 to prove the unavailable path; no schema or data change, and the migration set is untouched. Created GitHub issue #62 and the linked branch. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** Dependency health currently has one probe, the state store, because the SQL Server, AIMS API, and compatibility-read adapters do not exist yet; #11, #22, and #24 add their own probes against the published `DependencyProbe` protocol without changing the health service. A probe that raises is contained and its exception message is deliberately discarded, because a connection error commonly embeds the connection string; that means an operator sees "dependency check failed" rather than a cause, and richer diagnosis needs the structured logging in a later issue. One test defect was found and fixed: `Settings` intentionally reads `ESL_`-prefixed environment variables, so a unit test that omitted a key still passed when the developer shell exported it; those unit tests now clear ambient `ESL_` variables and are deterministic. Next action: open the PR to `develop` and record the cross-agent review; #62 completes the second half of AD-016 Task 7.
