- **Timestamp / owner:** 2026-08-31 20:30 +08:00; Claude issue #62 implementation.
- **Issue:** GitHub #62, [operations] add safe evidence retention and sanitized audit read models, assigned to it20pepito; labels type:feature, area:operations, area:persistence, area:web, priority:p2. Parent epic #7. Branch created with `gh issue develop`, so it is linked in the issue's Development panel.
- **Git state:** claude/62-retention-and-audit-reads in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-62-retention-and-audit-reads, branched from origin/develop at 61aabcb. Migration head verified as the single `0007_audit_reconciliation` before starting; this issue adds no migration.
- **Scope:** The second half of AD-016 Task 7 for NFR-007, FR-022, and architecture section 5.8. Added `persistence/retention.py` (RetentionPolicy with no duration defaults, RetentionRefused, PurgeOutcome, RetentionService.find_eligible and purge_execution), retention settings in `config.py` with a startup validator, and `web/audit_schemas.py` (ExecutionAuditResponse, RecordEvidenceResponse, ExecutionEventSummary, RecordIssueSummary). Updated architecture section 5.8 to record the implemented purge limitation. Non-goals honored: no retention duration chosen, purge disabled by default, no FastAPI route, no CSV delivery retention.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. RED was a real failure, not a skip: collection errors for the missing `esl_service.persistence.retention` and `esl_service.web.audit_schemas`. Unit green at 23 tests, retention integration green at 14. Full suite 330 passed with the dedicated database and 244 passed / 86 skipped without one, matching CI. Ruff passed; mypy passed for 34 source files; frontend typecheck, Vitest 1 passed, Vite build passed; `git diff --check` clean. No migration was added, so the graph stays single-headed at `0007_audit_reconciliation`.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. New settings `retention_purge_enabled` defaulting to false plus optional `audit_core_days`, `detailed_evidence_days`, and `compatibility_days`; no duration has a default and none was supplied. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Read and wrote only the dedicated non-production database `esl_pipeline_test` on localhost through rolled-back test transactions; the schema and migration set are unchanged. Created GitHub issue #64 and the linked branch. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** A conflict was found before implementation and reported rather than silently resolved: architecture section 5.8 classifies canonical snapshots as purgeable while actions are audit core, but `record_action.record_processing_result_id` and `record_processing_result.canonical_record_snapshot_id` are NOT NULL with RESTRICT, so retaining the audit core pins the snapshots. The approved decision was to purge only what the foreign keys allow; section 5.8 now records the limitation and issue #64 tracks the migration that removes it, deliberately without claiming a migration slot because `0008` is reserved for Task 8 and #21. Retention durations remain UNKNOWN / NEEDS-DISCOVERY, so purge stays disabled and refuses to run until the business supplies every applicable value. Next action: open the PR to `develop`, record the cross-agent review, merge once CI is green, then close #62 with the merge commit.
