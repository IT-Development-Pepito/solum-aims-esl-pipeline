- **Timestamp / owner:** 2026-08-31 21:30 +08:00; Claude issue #37 implementation.
- **Issue:** GitHub #37, [domain] preserve promotion compatibility selection and ambiguity audit, assigned to it20pepito; labels type:feature, area:domain, area:operations, priority:p1. Parent epic #5. Branch created with `gh issue develop`, so it is linked in the issue's Development panel.
- **Git state:** claude/37-promotion-compatibility-selection in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-37-promotion-compatibility-selection, branched from origin/develop at 02ba61e. Migration head verified as the single `0007_audit_reconciliation` before and after; this issue adds no migration.
- **Scope:** FR-004, FR-005, FR-006, FR-021, FR-022; BR-019 and BR-020. Added `domain/promotion_selection.py` with the versioned `compatibility-v1` strategy, `SELECTION_STRATEGY_VERSION`, and the two BR-019 reason codes; mapped both codes to BR-019 in `domain/validation.py` so they reach the record-issue trail and #25 reconciliation with the correct rule identifier; recorded the implemented strategy in the SPECIFICATION BR-019 row. Non-goals honored: no business winner rule approved or invented, no ambiguity suppressed, and the review-only SQL procedure is not treated as a production baseline.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. Followed superpowers:test-driven-development, which became available this session. Two red/green cycles with the failure observed each time: `ModuleNotFoundError: No module named 'esl_service.domain.promotion_selection'`, then the BR-019 mapping test failing with `- BR-019 / + BR-005`. Selection green at 18 tests, domain suite green at 171. Full suite 350 passed with the dedicated database and 264 passed / 86 skipped without one, matching CI. Ruff passed; mypy passed for 35 source files; frontend typecheck, Vitest 1 passed, Vite build passed; `git diff --cached --check` clean.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Created the linked branch only. The dedicated non-production database was read through rolled-back test transactions; its schema and migration set are unchanged. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** Two interpretations were confirmed with the user before implementation rather than assumed. First, the "approved compatibility case" clause is implemented as retaining an existing state only when it already equals an eligible candidate's outbound state, which changes nothing on the label and therefore makes no new business interpretation; no approved-case registry was invented. Second, an initial strict state comparison made the identical-state branch unreachable because campaign identity always differs, so comparison excludes campaign identity only, and any difference in type, value, prices, percentage, saving, raw text, group, or period still leaves the outcome unresolved. Deployed compatibility remains unclaimed: #38 and representative cases are still required. Formal campaign-priority policy, effective-price rounding, and same-economic term precedence remain UNKNOWN / NEEDS-DISCOVERY. Next action: open the PR to `develop`, record the cross-agent review, merge once CI is green, then close #37 with the merge commit.
