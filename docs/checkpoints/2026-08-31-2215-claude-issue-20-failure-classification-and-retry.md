- **Timestamp / owner:** 2026-08-31 22:15 +08:00; Claude issue #20 implementation.
- **Issue:** GitHub #20, [workflow] classify failures and apply bounded retry policy, assigned to it20pepito; labels type:feature, area:workflow, priority:p1. Parent epic #6. Branch created with `gh issue develop`, so it is linked in the issue's Development panel.
- **Git state:** claude/20-failure-classification-and-retry in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-20-failure-classification-and-retry, branched from origin/develop at 873202e. Migration head verified as the single `0007_audit_reconciliation` before and after; this issue adds no migration.
- **Scope:** FR-014 and FR-015. Added `domain/failures.py` with DependencyKind, FailureKind, FailureSignal, `classify`, UnclassifiedFailure, and RetryPolicy; added externalised retry settings and their startup validation to `config.py`, included in the sanitized configuration snapshot so the policy is audit-visible. The classification matrix is transcribed from SYSTEM_ARCHITECTURE section 8, one entry per documented row, each citing its source row. Non-goals honored: malformed data is never retryable and an unverified external submission is never resent automatically.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. Followed superpowers:test-driven-development. RED observed before implementation: `ModuleNotFoundError: No module named 'esl_service.domain.failures'`. Unit green at 32 tests, failure-injection integration green at 6. Full suite 388 passed with the dedicated database and 296 passed / 92 skipped without one, matching CI. Ruff passed; mypy passed for 36 source files after correcting an `int.__pow__` widening by using exact Decimal exponentiation; frontend typecheck, Vitest 1 passed, Vite build passed; `git diff --cached --check` clean.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. New settings `retry_max_attempts`, `retry_timeout_seconds`, `retry_initial_backoff_seconds`, `retry_max_backoff_seconds`, and `retry_jitter_ratio`, all validated as positive with the jitter ratio bounded to zero through one and the maximum backoff not below the initial delay. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Created the linked branch only. The dedicated non-production database was read and written through rolled-back test transactions; its schema and migration set are unchanged. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** Two limitations are recorded in PROGRESS Open Work. First, the retry defaults are provisional operational values, not measured targets; NFR-004 requires targets from a measured baseline and none has been captured, so they must be reviewed against measured latency and failure data before production acceptance. Second, concrete adapter fault mapping is deferred to adapters #11 and #22 through #24 as the issue's dependency note states; the matrix and policy are complete and independent of them. An unrecognised dependency and failure combination raises `UnclassifiedFailure` rather than defaulting, because a wrong default would either resend something unsafe or silently abandon recoverable work. Next action: open the PR to `develop`, record the cross-agent review, merge once CI is green, then close #20 with the merge commit.
