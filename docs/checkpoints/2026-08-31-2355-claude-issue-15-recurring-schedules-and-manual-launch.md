- **Timestamp / owner:** 2026-08-31 23:55 +08:00; Claude issue #15 implementation.
- **Issue:** GitHub #15, [workflow] add recurring schedules and auditable manual launch, assigned to it20pepito; labels type:feature, area:workflow, priority:p1. Parent epic #6. Depends on merged #14 and closed #1. Branch created with `gh issue develop`, so it is linked in the issue's Development panel.
- **Git state:** claude/15-recurring-schedules-and-manual-launch in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-15-recurring-schedules-and-manual-launch, branched from origin/develop at 35c2986. Migration head verified as the single `0007_audit_reconciliation` before and after; this issue adds no migration.
- **Scope:** FR-008. Added `domain/scheduling.py` with a five-field `CronExpression`, `ScheduleDefinition`, `decide_scheduled_launch`, `ManualLaunch`, the two execution builders, and the shared audit action vocabulary; added `persistence/launch_repository.py` with `LaunchRepository` for schedule creation, enable/disable, due-schedule selection, and both launch paths. Recorded the cadence constraint on the `workflow_schedule` row in SYSTEM_ARCHITECTURE section 5.4. Non-goals honored: no retry, replay, or status querying (#16), and no external AIMS mutation.
- **No migration was required.** `workflow_schedule`, `workflow_execution`, and `audit_entry` already carry every column FR-008 needs, including the nullable `audit_entry.execution_id` that exists precisely so a schedule action can be audited without a run. The one gap found is the architecture's "unique active schedule identity per configured workflow/store", which the table does not enforce. It was deliberately not taken: the AD-016 plan assigns final indexes and checks to the reserved `0008_authoritative_model_gate` under #21, and consuming that slot here would break the reserved sequence. It is recorded in PROGRESS Open Work instead.
- **Acceptance criteria:** A disabled schedule creates no run is enforced in the domain (`decide_scheduled_launch` refuses before timing is considered) and verified against real rows, including that `due_schedules` uses the same decision so the two cannot disagree. An authorized manual launch creates an execution with identity and reason is enforced by `ManualLaunch`, which refuses a blank operator or reason before any row is written. Schedule configuration, enable/disable changes, and launch source are audit-visible through three audited actions, with before/after evidence on every enable/disable.
- **Two judgement calls, both stated rather than assumed.** Refusals are not audited: a cadence is evaluated once a minute, so recording every not-due or disabled tick would bury the operator actions FR-022 exists to surface, and the enable/disable entry already explains why nothing ran. A timed run is audited with actor `scheduler` and the schedule id as evidence, rather than borrowing the schedule's UUID as an actor, because an actor names who acted and a schedule is not an identity.
- **Authorization boundary:** #15 establishes that an operator identity and a reason exist and are recorded. It does not evaluate whether that operator is permitted to launch, which is FR-023 role checking and belongs to #26. This is stated in the module docstring so the gap cannot be mistaken for coverage.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. Followed superpowers:test-driven-development. RED observed before each implementation step and for the expected reason: `ModuleNotFoundError: No module named 'esl_service.domain.scheduling'`, then `ModuleNotFoundError: No module named 'esl_service.persistence.launch_repository'`. One of my own test helpers was wrong and was corrected rather than the source being loosened: it built a Jakarta instant as `hour - 7`, which raised `ValueError: hour must be in 0..23` for hours before 07:00; it now constructs the local time with `ZoneInfo` and converts. Domain 40 passed, launch integration 12 passed. Commands and results: `python -m ruff check src tests` passed after fixing an unsorted import block, a `getattr` on a frozen dataclass replaced with declared `init=False` fields, and naive datetimes in the cron tests made timezone-aware except the one case that deliberately asserts a naive instant is refused; `python -m mypy src` reported no issues in 40 source files; `python -m pytest -q` reported 461 passed with the dedicated database and 357 passed / 104 skipped without one, matching CI; frontend `npm run typecheck`, `npm run test -- --run` at 1 passed, and `npm run build` all passed; `git diff --check` clean.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. No new settings were added. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Created the linked branch only. The dedicated non-production database was read and written through rolled-back test transactions; its schema and migration set are unchanged. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** Three limitations are recorded in PROGRESS Open Work: the unenforced unique schedule identity deferred to #21's reserved `0008`; authorization deferred to #26; and the absence of a timed loop, since nothing ticks the cadence until #28 hosts the service, so a schedule currently launches only when a caller supplies the instant. The supported cron subset is deliberately narrow and rejects what it cannot read, including `@` shorthands and month or weekday names, because a silently mis-parsed cadence would run the wrong workflow at the wrong time while looking configured. The VERIFIED legacy cadence, every 30 minutes from 07:00 through 23:59, is expressible as `*/30 7-23 * * *` and is covered by test. While updating Open Work I corrected a stale line that described merged issue #37 as open, and noted that the Completed list is behind by #37, #20, and #22. Next action: open the PR to `develop`, record the cross-agent review, merge once CI is green, then close #15 with the merge commit.
