- **Timestamp / owner:** 2026-09-01 09:14 +08:00; Codex issue #16 implementation start.
- **Issue:** GitHub #16, `[operations] add workflow status, retry, and bounded replay controls`, assigned to `it20pepito`; labels `type:feature`, `area:workflow`, `area:operations`, `priority:p1`; dependencies #14 and #1 are closed.
- **Git state:** `codex/16-workflow-retry-replay-controls` in `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-16-workflow-retry-replay-controls`, created from `origin/develop` at `ed53e17`; clean before this checkpoint; no commit or PR yet.
- **Scope:** Implement FR-011/FR-012 status retrieval, failed-run retry, and explicit bounded replay using the existing execution, audit, action, and scope-ownership models. Retry/replay create linked executions and require operator identity/reason. API/CLI authorization, scheduler policy, automatic retry algorithms, runtime hosting, migrations, and external-system interaction are non-goals.
- **Evidence:** The approved documents, issue #16 metadata, neighboring #26/#28 boundaries, existing implementation/tests, and migration graph were inspected. Alembic reports exactly one head, `0007_audit_reconciliation`. Failing requirement-traceable tests are the next action.
- **Configuration:** Use the existing root `.venv` and root `.env` without reading or copying secret values. No configuration contract changes are planned.
- **External state:** Repository/GitHub reads and the local branch/worktree creation only. No PostgreSQL data mutation yet; no SQL Server, AIMS, Jenkins, Hop, CSV-delivery, ESL-device, or production state accessed or changed.
- **Risks / next action:** A retry must be refused when the original is not `FAILED` or contains an `OUTCOME_UNKNOWN` action; replay must reject missing, naive, or inverted bounds. Next action: write domain and PostgreSQL integration tests, run them red, then implement the smallest passing change.
