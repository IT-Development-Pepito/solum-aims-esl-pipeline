- **Timestamp / owner:** 2026-09-01 09:30 +08:00; Codex issue #16 focused-green checkpoint.
- **Issue:** GitHub #16, `[operations] add workflow status, retry, and bounded replay controls`, assigned to `it20pepito`; FR-011 and FR-012 only.
- **Git state:** `codex/16-workflow-retry-replay-controls` in `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-16-workflow-retry-replay-controls`, based on `origin/develop` at `ed53e17`; implementation remains uncommitted and no PR exists yet.
- **Scope:** Added pure operator/replay/query validation, failed-only retry eligibility, unresolved-external-action refusal, execution status filters, linked retry/replay launches through the existing scope owner, and structured launch/refusal audit evidence. No API, CLI, authorization, scheduling, runtime, model, or migration change.
- **Evidence:** RED domain baseline failed collection because `esl_service.domain.operations` did not exist. RED integration baseline failed because `WORKFLOW_RETRY_REFUSED` did not exist. After implementation and formatting: focused Ruff passed; focused mypy passed for 3 source files; `pytest tests/unit/domain/test_operations.py tests/integration/test_execution_operations.py -q` reported `21 passed in 0.22s`; `git diff --check HEAD` passed.
- **Configuration:** Reused the root `.venv` and loaded only `ESL_TEST_DATABASE_URL` from the ignored root `.env` into the test process; the value was not printed, copied, logged, or committed. `PYTHONPATH` pointed at this worktree because the shared editable install points at the root checkout.
- **External state:** Tests migrated/used only the configured dedicated non-production PostgreSQL database and rolled back test rows through existing fixtures. No SQL Server, AIMS, Jenkins, Hop, CSV-delivery, ESL-device, or production system was accessed or changed.
- **Risks / next action:** The operator-facing authorization/API/CLI remains intentionally deferred to #26/#28. Next action: run the complete backend/frontend verification gate, review the final diff, and record the completion checkpoint before commit.
