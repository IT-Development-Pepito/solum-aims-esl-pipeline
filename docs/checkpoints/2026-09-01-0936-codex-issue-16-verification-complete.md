- **Timestamp / owner:** 2026-09-01 09:36 +08:00; Codex issue #16 verification-complete checkpoint.
- **Issue:** GitHub #16, `[operations] add workflow status, retry, and bounded replay controls`, assigned to `it20pepito`; implements FR-011 and FR-012 within the approved scope.
- **Git state:** `codex/16-workflow-retry-replay-controls` in `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-16-workflow-retry-replay-controls`, based on `origin/develop` at `ed53e17`; eight approved files are staged before adding this checkpoint; no commit or PR yet.
- **Scope:** Final staged implementation provides bounded/composable execution status queries, failed-only linked retry with unresolved-action refusal, explicit bounded linked replay, operator identity/reason validation, existing scope-ownership enforcement, and immutable success/refusal audit evidence. No persistence model, migration, API, CLI, authorization, scheduler, runtime, adapter, or external-system behavior changed.
- **Evidence:** Fresh final gate from the reviewed staged state: `python -m ruff check src tests` passed; `python -m mypy src` reported success for 42 source files; `python -m pytest -q` reported `534 passed in 3.82s`; frontend `npm run typecheck` passed; `npm run test -- --run` reported 1 test file and 1 test passed; `npm run build` completed with 16 modules transformed; `git diff --cached --check` passed. Alembic still has exactly one head, `0007_audit_reconciliation`.
- **Configuration:** Reused the ignored root `.env` and existing root `.venv`; only `ESL_TEST_DATABASE_URL` was loaded into test processes and its value was never displayed or copied. The ignored worktree `frontend/node_modules` junction points to the existing root dependency installation; no dependency was installed or changed.
- **External state:** Full tests used only the dedicated non-production PostgreSQL database and rollback-based fixtures. No SQL Server, AIMS, Jenkins, Hop, CSV delivery, ESL device, or production system was accessed or changed.
- **Risks / next action:** Authorization and transport exposure remain deferred to #26/#28 as designed. Next action: stage this checkpoint, re-run `git diff --cached --check`, commit with issue traceability, push, open a PR to `develop`, link #16, and obtain cross-agent review plus green CI before merge.
