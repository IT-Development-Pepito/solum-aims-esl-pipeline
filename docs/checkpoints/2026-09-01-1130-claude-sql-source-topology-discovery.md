- **Timestamp / owner:** 2026-09-01 11:30 +08:00; Claude source-topology discovery.
- **Issue:** None. The finding emerged while scoping #38 and #24 access with the source owner. It is documentation only; no requirement, rule, or behaviour changed. Raised issues #78 and #79 separately for the credential work it exposed.
- **Git state:** claude/docs-source-topology in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\docs-source-topology, branched from develop at 11f5100. No code, schema, test, or migration touched. Alembic head unchanged at the single `0007_audit_reconciliation`.
- **Scope:** Record the real source topology of `dbo.RefreshESL_New` as VERIFIED discoveries, correct the two SYSTEM_ARCHITECTURE inventory rows that misstate it, and record three consequences in Open Work: #11's scope no longer matches reality, the read-versus-business-logic boundary is decided, and credential handling is tracked by #78 and #79.
- **How the finding was made, including my own errors:** The supplied procedure is UTF-16LE, so an ordinary grep silently matched nothing; it had to be decoded first. My first extraction reported "19 objects, all in the `ESL` database", which was **wrong**: it read only static two-part names, while the real campaign, item, and stock reads are four-part names assembled by string concatenation at runtime and therefore invisible to a literal search. A second error followed: a bracketed-name regex matched the last three segments of `[192.168.85.18].[PEPITO_HO].dbo.ITEM_UOM_MAPPING_MST`, so `PEPITO_HO` was recorded as a same-instance three-part reference when it is remote. Both were corrected before anything was written to the documents. The final inventory was produced by marking every line inside a `/* */` block and re-extracting, which is also what surfaced the dead code.
- **The topology:** Three live tiers, sixteen live objects. Same instance as `ESL`: `DBWH_8555` by three-part name, supplying `DimStore`, `DimItemMapping`, and `FactCampaign`. Remote through a linked server: `PEPITO_HO`, the central iRetail database, supplying `ITEM_UOM_MAPPING_MST`. Remote through one linked server per store: a local iRetail SQL Server supplying twelve campaign, item, stock, and movement tables. Every target is SQL Server and one read-only account applies to all, both confirmed by the source owner.
- **Per-store targets are data, not configuration.** The procedure reads `ORG_IP` and `ORG_DB` from `DBWH_8555.dbo.DimStore` per store code and builds four-part names by concatenation, so the store set is discovered at runtime and `ORG_IP` holds a bare IP with no port. This matches FR-026, where adding a store is data rather than code, but it also means a connection target originates from table content. That is the only place in the system where this is true, so #78 carries an acceptance criterion requiring the value to be validated as an IP or hostname before use.
- **Three objects are dead code:** the `STORE_OPS_APP` linked server, the `benchmark_master_selected_items` table it fronts, and `DimItem`. All three appear only inside a commented-out `REDLIST` update. Removing them from scope eliminated one external dependency, one credential, and one open question. As a direct consequence `tb_ESL.REDLIST` is never populated and keeps the empty string it is initialised with; whether the field is retired or temporarily disabled is UNKNOWN / NEEDS-DISCOVERY. The source owner directed that the current behaviour be replicated for now, so nothing is invented.
- **`tb_ESL` changes role.** SYSTEM_ARCHITECTURE classified it as "SQL read model/source compatibility", which now reads as though the replacement consumes it. It does not. The replacement reads the same three tiers the procedure reads, computes in the domain, and writes only its own PostgreSQL; `tb_ESL` is read solely as the parity baseline under `ESL_SHADOW_MODE`. The row was corrected rather than left ambiguous, because an implementer following the old wording would build the wrong adapter.
- **Boundary decision recorded:** adapters fetch raw rows bounded only by store and source window; the domain applies every predicate carrying business meaning, such as campaign status, campaign type membership, validity dates, and the PFS exclusion. The trade-off is a larger result set, accepted deliberately: a predicate in SQL discards rows without a trace, whereas a predicate in the domain records each rejection as a `record_issue` with its reason. That difference is the whole point of the replacement, and much of the rule set already exists in Python from #36, #37, and #12.
- **Evidence:** Findings were extracted from `docs/sql-server/Store_Procedure_Refresh_ESL.sql` at commit 11f5100 by decoding UTF-16LE and analysing comment regions; line numbers were verified individually. No database was contacted; access to SQL Server and AIMS is not yet provisioned. `python -m ruff check src tests`, `python -m mypy src`, `python -m pytest -q`, the frontend gates, and `git diff --check` were run to confirm the documentation-only change breaks nothing; results are in the pull request.
- **Configuration:** None read or changed. The one IP address named in this checkpoint already exists in the committed procedure evidence; no new value is introduced and no credential appears.
- **External state:** One branch and one worktree created. No PostgreSQL, SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated.
- **Risks / next action:** The main risk this records is that #11 is currently selectable but its scope is wrong; an implementer taking it as written would build a single-source adapter against a three-tier reality. Its rewrite is proposed as a comment on the issue rather than applied unilaterally, because resizing a P1 issue is the owner's call. Two credential prerequisites are now tracked: #79 supplies the missing bundle writer and #78 routes every password through it. Next action: open the PR to `develop`, record the cross-agent review, merge once CI is green, then agree the #11 rewrite before any adapter work starts.
