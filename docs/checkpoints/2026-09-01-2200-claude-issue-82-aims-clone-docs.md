- **Timestamp / owner:** 2026-09-01 22:00 +08:00; Claude issue #82 documentation.
- **Issue:** GitHub #82, [docs] document the local AIMS clone procedure and its failure modes, assigned to it20pepito; labels type:docs, area:docs, area:adapters, priority:p2. Supports #24. Carried on the #79 branch and pull request by the owner's decision, as its own commit, since the two do not touch each other.
- **Git state:** claude/79-82-secret-tooling-and-aims-clone-docs in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-79-82-secret-tooling, second commit on top of the #79 commit e84cc53. Documentation only; no code, schema, test, or migration touched. Alembic head unchanged at the single `0007_audit_reconciliation`.
- **Scope:** NFR-011 and AD-003. Added `docs/development/aims-local-clone.md`, a new `docs/development/` area for developer-facing procedures that are neither operator runbook nor current-state evidence. Added a pointer from the daily development workflow in `WORKFLOW.md`, and replaced the #82 entry in PROGRESS Open Work. Non-goals honored: no automation of the clone, no refresh schedule, no adapter implementation, and no change to production AIMS.
- **What the document records, and why each part earns its place.** The working `pg_dump` and `pg_restore` invocations, and the one verification step that was missing from every failed attempt: `pg_restore --list` before restoring. Two symptoms that look like different problems and are the same one, a data-only archive, which presents as foreign-key violations against an existing schema and as `relation does not exist` against an empty one. The `postgres_fdw` dependency of Portal on Core, why the materialized view refresh fails on a fresh clone, and the decision to leave it unpopulated because the adapter reads both databases directly. The `end_device` versus `enddevice` naming trap. A `SELECT`-only `esl_aims_reader` role that mirrors the production identity so #24's least-privilege criterion can be exercised locally, with the reminder that grants are per database because forgetting the second one was itself mistaken for a restore problem. And the `information_schema` privilege filter, which returned zero rows and briefly looked like the tables were absent.
- **Acceptance criteria:** the procedure is complete enough to rebuild both databases from the document alone, with expected table counts to confirm success; each of eight failure modes records symptom, cause, and fix in the order they were met; the document states that the dump reads production AIMS and must run off-peak and that the restore target is always local; and no credential, password, or production host address appears anywhere in it, with placeholders where a value must be supplied.
- **Evidence:** Every command and every failure in the document was actually run or actually hit during this session against the local clone and the owner's dump attempts; nothing is reconstructed from memory of how PostgreSQL usually behaves. The table counts, 56 plus 3 foreign tables for Portal and 20 for Core, were read from the restored databases and match the vendor DDL in `docs/hop-jenkins-pipeline/`. `python -m ruff check src tests`, `python -m mypy src`, `python -m pytest -q`, the frontend gates, and `git diff --check` were run on the branch to confirm the documentation-only commit breaks nothing; results are in the pull request.
- **Configuration:** None read or changed. The document deliberately uses `<placeholders>` for the AIMS host, the read-only user, and every local password.
- **External state:** None. The local clone was read for the table counts; production AIMS was not contacted. No PostgreSQL, SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was mutated.
- **Risks / next action:** The clone is a snapshot with no defined refresh cadence, and the document says so; a cadence becomes necessary once the `page` question is settled and shadow-mode comparison starts. `docs/development/` is a new directory not yet listed in the repository layout in `AGENTS.md`; that file is deliberately kept concise and the addition is small, so it is left for the owner to decide whether the layout list should name it. Next action: open the pull request carrying both commits, record the cross-agent review, and merge once CI is green.
