- **Timestamp / owner:** 2026-09-02 00:30 +08:00; Claude issue #78 implementation.
- **Issue:** GitHub #78, [runtime] route every credential through the DPAPI bundle and verify connectivity, assigned to it20pepito; labels type:security, area:adapters, area:runtime, priority:p1, and `blocked` at start because #79 had not merged. Blocks #11 and #24. Branch created with `gh issue develop 78`, so it is linked in the issue's Development panel.
- **Git state:** claude/78-credentials-through-bundle in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-78-credentials-through-bundle, branched from develop at da3cb15, the #79/#82 merge. Migration head verified as the single `0007_audit_reconciliation` before and after; this issue adds no migration.
- **Scope:** NFR-009, AD-007, AD-017, FR-024, FR-025. `Settings` gains the non-secret parts of every source and AIMS connection: host, database, username, driver, TLS trust, and the AIMS port. There is deliberately no field that could hold a password, and a test asserts none exists. Four fixed bundle keys are the only place a password lives: `state.password`, `source.sql.password`, `aims.portal.password`, `aims.core.password`. `targets_from_settings` derives six targets, `store_target` builds a validated per-store target, `ConnectivityProbe` and `build_probes` report every target through the existing `HealthService`, `describe_secret_problems` names a missing key at startup, `create_database_engine_from_settings` takes the state-store password from the bundle, and both `.env` examples are password-free. Non-goals honored: no adapter (#11, #24), no rotation, no permission change on SQL Server or AIMS, no monitoring platform.
- **The one pre-existing exception is closed at the gate, not in the model.** `ESL_DATABASE_URL` used to embed its password. `validate_startup_configuration` now refuses a URL with one, naming the key and the remedy without echoing the value, and `create_database_engine_from_settings` refuses the same URL rather than merging two sources of truth for one credential. `Settings.model_validate` itself stays permissive, so the existing snapshot-redaction test keeps a secret-bearing URL to redact and unit fixtures across the suite keep working. Exactly one existing test, the one asserting a valid configuration reports no problem, had to switch to a password-free URL; nothing else moved.
- **Per-store addresses are validated because they are data, not configuration.** `DimStore.ORG_IP` and `ORG_DB` reach the adapter as row values, the only place in the system where a connection target originates in a table. `store_target` accepts a bare IPv4 address or a hostname of dot-separated labels and a plain-identifier database name, and refuses everything else, including a port suffix, because `ORG_IP` holds no port, confirmed by the source owner. `STORE_OPS_APP` is absent by test: its only use in the procedure is commented out.
- **Health integration reuses #27 rather than adding a second mechanism.** One `ConnectivityProbe` per target; only the state store is `required`, so a source outage never makes a running service report itself unready, since it can still serve status and audit. An unconfigured target is DEGRADED rather than UNAVAILABLE, so a gap in configuration is visible without being counted as an outage. One assertion in my own test was wrong and was corrected against #27's existing rule rather than the rule being changed: the aggregate report state reflects the worst dependency even when readiness holds, and that is right because an operator must still see the outage.
- **Evidence:** Python 3.12.7, Node 22.23.2, PostgreSQL 18.6. Followed superpowers:test-driven-development. RED observed first: four of six new test files failed collection on the expected missing names, `AIMS_CORE_PASSWORD_KEY`, `ConnectivityProbe`, `create_database_engine_from_settings`, and `describe_secret_problems`. First GREEN run over the new and affected files: 149 passed, 2 failed. One failure was my wrong aggregate-state assertion, corrected as above; the other was the existing snapshot test meeting the new gate, corrected by giving that one call a password-free URL. `python -m mypy src` reported no issues in 45 source files. The final gate results, ruff over source and tests, the full suite with and without the dedicated database, the frontend checks, and `git diff --check`, are recorded in the pull request.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, read from the existing developer `.env` into a gitignored worktree `.env`; never printed or committed. It is a test-only variable, not a `Settings` field, and the dev example now says so explicitly because it is the one URL that legitimately still carries a password. No production configuration, secret bundle, or DPAPI material was read.
- **External state:** Created the linked branch only. No PostgreSQL, SQL Server, AIMS, Jenkins, Hop, CSV delivery, device, or production state was read or mutated; engine creation in tests opens no connection.
- **Risks / next action:** The SQL Server targets are built and probed against synthetic errors only; no SQL Server is reachable from the development machine, so their first real exercise waits for the provisioned read-only identity and route. Store enumeration from `DimStore` is #11's work; #78 supplies only the validated target builder it will call. Once merged, #78's own `blocked` label should be removed and #11 and #24 lose their credential blocker, leaving #38 and the network routes. Next action: open the PR to `develop`, record the cross-agent review, and merge once CI is green.
