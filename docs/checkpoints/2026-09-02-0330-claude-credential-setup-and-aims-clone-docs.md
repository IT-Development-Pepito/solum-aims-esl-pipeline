- **Timestamp / owner:** 2026-09-02 03:30 +08:00; Claude onboarding documentation.
- **Issue:** None, by the owner's instruction. Documentation only. The gap was found the practical way: the owner, provisioning credentials for the first time, asked three questions in a row that no document answered — what `state.password` is the password of, which passwords are typed raw and which are percent-encoded, and when `esl-admin secrets set` is run. The mechanics were documented after #79 and #78; the onboarding path was not.
- **Git state:** claude/docs-credential-setup-and-aims-clone in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\docs-credential-setup, branched from develop at 875f097, the #24 merge. No code, schema, test, or migration touched. Alembic head unchanged at the single `0007_audit_reconciliation`.
- **Scope:** Three deliverables, all requested explicitly. A new `WORKFLOW.md` procedure, "Set up credentials for a new environment", placed before the existing rotation procedure, which now reads as the rotation reference rather than the onboarding path. A `README.md` that was a single title line and now carries the full credential setup and the full AIMS clone walkthrough, since the owner asked for both to be there in detail rather than only linked. No change to `PROGRESS.md`: nothing about the project's phase, decisions, risks, or discoveries moved.
- **What the new material adds that the existing procedure lacked:** the four-step order for a new environment (accounts, `secrets set` times four, `check-connections`, start); the key-to-user-to-database table; the raw-versus-encoded rule, which is the single point that caused the most confusion, stated as a two-row table with the only three `.env` variables that ever carry a password named explicitly; a screen example showing exactly where the raw value is typed; and the four events that trigger `secrets set` again, with the statement that it is never part of startup and never scheduled.
- **The README clone walkthrough is a walkthrough, not a copy.** It carries the six steps with the expected result at each, the one expected error on Portal and why it is ignored, the grants-are-per-database reminder that was itself once mistaken for a restore problem, and the three failures a first-timer is most likely to hit. The eight-row failure table and the FDW repointing detail stay in `docs/development/aims-local-clone.md`, which the README links as the reference.
- **Evidence:** Every command in both documents was run during this project against real systems: the credential commands against a demo bundle and the local state store earlier this session, the clone commands against production AIMS and the local PostgreSQL 18 while producing the first clone. The `check-connections` outcome table matches the vocabulary in `runtime/connectivity.py`. The verification gate was run to confirm the documentation-only change breaks nothing; results are in the pull request.
- **Configuration:** None read or changed. Both documents use placeholders for every host, user, and password, and name only the four bundle keys and the three test-URL variable names.
- **External state:** One branch and one worktree created. Nothing else read or mutated.
- **Risks / next action:** README and WORKFLOW now describe the same procedure in two places by request, which means a future change to the key names or the setup order must be made in both; the WORKFLOW section says so by pointing at README, and the reverse link exists. The README's expected `check-connections` output shows every tier `REACHABLE`, which is the target state; on the owner's machine today the SQL Server tiers are `UNCONFIGURED` until hosts and the shared password exist, and the AIMS pair was `CREDENTIAL_REJECTED` because the local `esl_aims_reader` password changed after the #24 tests ran. Next action: open the PR to `develop` and merge once CI is green.
