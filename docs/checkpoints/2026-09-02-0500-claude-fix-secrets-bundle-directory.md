- **Timestamp / owner:** 2026-09-02 05:00 +08:00; Claude fix to the #79 secret tooling.
- **Issue:** None, by the owner's instruction; a defect in the #79 deliverable found in use. The owner's first real `esl-admin secrets set` on a machine without `C:\ProgramData\SOLUM\ESL` ended in a raw `FileNotFoundError` traceback from `pathlib.write_bytes`. The tool promised controlled, non-disclosing failures, and a `PermissionError`, which is exactly what a wrong production ACL produces, would have escaped the same way.
- **Git state:** claude/fix-secrets-bundle-directory in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\fix-bundle-directory, branched from develop at f1a5e18, the #88 merge. No schema or migration touched; head unchanged at the single `0007_audit_reconciliation`.
- **Scope:** Two behaviours in `runtime/cli.py`, the one procedure sentence they imply in `WORKFLOW.md` and `README.md`, and four tests. A missing bundle directory is created and announced on a development machine, where no service identity is configured, and refused with a clear message when one is. Any `OSError` from the store, `FileNotFoundError` and `PermissionError` included, becomes a fixed message and exit code 1 instead of a traceback. `secrets list` needed no change: a missing bundle already reports "unavailable" through the reader's own rule.
- **Why the directory is never created silently in staging or production.** The startup validator checks the bundle directory's owner and ACL, not only the file's. A folder this tool created would carry inherited permissions, be accepted here, and then be rejected by the service at start, with the deliberately non-disclosing `secret bundle is unavailable` as the only clue. Refusing at write time with the remedy in the message is the honest failure. The dev/production split reuses the identity guard's existing distinction rather than inventing a flag.
- **Evidence:** Followed superpowers:test-driven-development. RED observed first: three of four new tests failed because an `OSError` escaped the command; the fourth, `remove` on a missing directory, passed immediately because that path never writes and returns "not present" before touching the filesystem. It is kept as a regression guard for the adjacent path and is named as a pass-first test here rather than presented as if it had driven code. GREEN: 24 passed across the new file and the two existing CLI test files. `python -m ruff check src tests` passed; `python -m mypy src` reported no issues in 47 source files. The full gate, the suite in its CI shape, the frontend checks, and `git diff --check`, was run before commit; results are in the pull request.
- **Configuration:** None read or changed. The tests use temporary directories only.
- **External state:** One branch and one worktree created. Nothing else read or mutated.
- **Risks / next action:** The `PermissionError` path is exercised through a fake protector that raises, not through a real denied ACL; a real denial on `C:\ProgramData` as a non-administrator would exercise the same `except OSError` branch, since `write_bytes` raises the same class. The owner can proceed immediately by creating the directory once, which the refusal message now says. Next action: open the PR to `develop` and merge once CI is green; the owner's provisioning then continues from step 3 of the README order.
