- **Timestamp / owner:** 2026-09-02 07:00 +08:00; Claude fixes to the #78/#79 diagnostics.
- **Issue:** None, by the owner's instruction; three defects found while the owner provisioned credentials for real. Each hid its cause behind a message that could not be acted on, and each cost a round of reproduction outside the tool to diagnose.
- **Git state:** claude/fix-connectivity-and-audit-diagnostics in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\fix-diagnostics, branched from develop at 7a9ce15, the #89 merge. No schema or migration touched; head unchanged at the single `0007_audit_reconciliation`.
- **Defect 1, a missing ODBC driver reported as a route fault.** `ESL_SOURCE_SQL_DRIVER` still held the URL-encoded `ODBC+Driver+18+for+SQL+Server` from the old design, so ODBC looked for a driver literally named that, answered `IM002`, and `check-connections` said `UNREACHABLE` for all three SQL Server tiers. `IM002` is raised before any network activity. It is now its own outcome, `DRIVER_MISSING`, whose detail names the driver string that was looked for, so a stray `+` or a typo is visible in the report itself. It maps to `UNAVAILABLE` in the health report and is not `ok`.
- **Defect 2, every audit failure called "the state store is unavailable".** The owner's `ESL_DATABASE_URL` pointed at `esl_pipeline_dev`, which had never been migrated: zero tables, no `alembic_version`. The connection succeeded, so `check-connections` said `REACHABLE`; the `INSERT` into `audit_entry` failed with `undefined_table`, and one broad `except` turned that into a message about availability. `_record_audit` now returns a classified `AuditFailure` or `None`, and `_audit_or_warn` prints one remedy per cause: schema not migrated, credential refused, bundle key missing, URL still embeds a password, unreachable, or configuration not loadable. The classifier reads SQLSTATE `42P01` through SQLAlchemy's `orig`, reuses `classify_failure` for the credential case, and never echoes the driver's text. Three existing test fixtures moved from `True`/`False` to `None`/`AuditFailure`; none was weakened.
- **Defect 3, migrations needed the password the service refuses.** `alembic/env.py` read `ESL_DATABASE_URL` verbatim, so migrating the development store required a password-bearing URL, exactly what the #78 startup gate rejects for the service. `persistence/migration_url.py` now resolves the URL the way the service does: a URL that already embeds a password is used unchanged, which keeps the integration fixtures and CI working, and otherwise `state.password` is read from the bundle and injected onto the parsed URL, rendered with the password percent-escaped. A missing key is a `RuntimeError` naming the key and the `esl-admin` command, never the bundle path. The resolution is a separate module so it is testable without Alembic's runtime.
- **Documentation:** the setup order in `WORKFLOW.md` and `README.md` gains the step it was missing, `alembic upgrade head` between provisioning and the connectivity check, with the note that until it runs `secrets set` stores the secret but warns. The `check-connections` outcome tables gain `DRIVER_MISSING`.
- **Evidence:** Followed superpowers:test-driven-development. RED observed first: two of three new test files failed collection on `AuditFailure` and `migration_url`; the driver-missing file collected and failed on the absent enum member. GREEN: 48 passed across the three new files and the three fixture-updated CLI files. `python -m ruff check src tests` reported three findings, one a deliberate `raise Exception` in a test's fake connector, replaced by a named `OdbcInterfaceError`, and two import-order fixes; then clean. `python -m mypy src` reported no issues in 48 source files. `python -m pytest -q` reported 695 passed and 9 skipped with the dedicated database, the skips being the AIMS-clone tests whose URLs were not exported for that run, and 572 passed / 132 skipped with no database, the CI shape. The frontend was not touched, so its gate was not run; `git diff --cached --check` reported nothing.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, exported explicitly for the run, never printed or committed. No production configuration or bundle was read.
- **External state:** One branch and one worktree created. The dedicated test database was read and written only inside rolled-back test transactions. The owner's `esl_pipeline_dev` was not migrated by this work; that is the owner's step, now documented, and the reason for the warning they saw.
- **Risks / next action:** `DRIVER_MISSING` is exercised through a fake connector shaped like pyodbc's `InterfaceError`; the real `IM002` was observed directly during diagnosis, so the shape is known, not assumed. `resolve_migration_url` is unit-tested; its wiring in `alembic/env.py` is exercised by the integration fixtures' password-bearing path in CI and by the owner's first `alembic upgrade head` against `esl_pipeline_dev` for the bundle path. Next action: open the PR to `develop`, merge once CI is green, then the owner runs `alembic upgrade head` with a password-free `ESL_DATABASE_URL` and re-runs `secrets set`; the warning should not appear.
