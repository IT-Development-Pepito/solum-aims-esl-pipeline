- **Timestamp / owner:** 2026-09-02 10:00 +08:00; Claude implementation of #26.
- **Issue:** #26, authorize and audit safe manual workflow operations (FR-023). Selected by the owner on 2026-09-02 after the three decisions below were put to them explicitly, because no document defined a role vocabulary, an identity source, or what "fallback" meant, and inventing any of the three would have been a business rule.
- **Decisions obtained before implementation (AD-018):** two roles, `operator` and `admin`; identity supplied by the caller with roles from configuration until #28 authenticates; `fallback` is the in-application part of the SPECIFICATION section 8 cutover rollback. All three were the recommended options. The issue's dependency on the #28 runtime interfaces is circular with #28's dependency on #26; the owner chose to build the authorization layer first, so nothing in this change exposes the service through a CLI or web surface.
- **Git state:** claude/operations-authorized-manual-operations in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\ops-authz, created with `gh issue develop 26` from develop at 83b668d, the #90 merge. No schema or migration touched; head unchanged at the single `0007_audit_reconciliation`. The audit rows this change writes use the existing `audit_entry` table.
- **What was built.** `domain/authorization.py`: `Role`, `Operation`, `REQUIRED_ROLE`, `Principal`, `authorize`/`require`, `NotAuthorized` carrying the decision, and `parse_role_assignments`/`principal_for` for the `identity=role[,role];...` text, with Windows-style case-insensitive identities and refusal of unknown roles, missing sides, and duplicate identities. `application/operations.py`: `AuthorizedOperations`, one service for all eight FR-023 operations, whose ports are the shapes of the existing `LaunchRepository`, `ExecutionRepository`, and `ReconciliationRepository`, so persistence is unchanged apart from one new query, `schedules_for_scope`. A refusal is audited as `operation.refused` under the principal's own name with the operation, required role, held roles, and policy version, then raised. Trigger, retry, replay, and schedule changes are audited by the repositories already; the service adds entries only where none would exist: refusal, `reconciliation.requested`, and `fallback.applied`. `config.py`: `operator_roles` setting, `build_role_assignments`, and a startup-gate problem for a malformed mapping. `runtime/principals.py`: `current_principal` joins the running account to its configured roles.
- **Fallback semantics:** disable every enabled schedule in the workflow or workflow-and-store scope, each as its own audited change under the admin's identity; touch no execution, checkpoint, or audit row; append one `fallback.applied` entry whose before/after evidence lists the schedule ids and whose `reconcile_window_from` is the instant the fallback was applied. A no-op fallback is still recorded. Restoring the legacy SQL Agent trigger, reconciling the window, and opening the incident record are documented as manual.
- **Evidence:** Followed superpowers:test-driven-development. RED observed first: all four new test files failed collection on the absent `domain.authorization`, `application.operations`, `build_role_assignments`, and `runtime.principals`. GREEN: 75 passed across the four files plus the architecture boundary tests, which confirm `application` still imports no persistence or transport. `python -m ruff check src tests`: one import-format finding, auto-fixed, then clean. `python -m mypy src`: no issues in 51 source files, after one `list` invariance fix in the refusal evidence. `python -m pytest -q`: 762 passed / 9 skipped with the dedicated database (the skips are the AIMS-clone tests, whose URLs were not exported for this run), 634 passed / 137 skipped without one, the CI shape. The frontend was not touched, so its gate was not run. `git diff --cached --check` reported nothing.
- **Configuration:** `ESL_TEST_DATABASE_URL` only, exported for the run and never printed. No production configuration, bundle, or Windows account was read; `current_principal` is tested with an injected account name.
- **External state:** One branch and one worktree created, and a per-worktree virtual environment. The dedicated test database was read and written only inside rolled-back test transactions. Issues #91 to #94 were created earlier the same morning to supersede #11; they are unrelated to this change.
- **Risks / next action:** The role model is deliberately minimal and versioned; if the business later needs a `viewer` or per-store scoping, the policy version changes and old audit rows stay interpretable. `status` requires a role but no reason, since a read has no evidence to justify; if the owner wants reads audited too, that is one added entry. The `held_roles` evidence names roles, never anything secret. Next action: open the PR to `develop`, cross-agent review, merge once CI is green, then #28 can expose the service through the Windows Service and CLI hosting and replace `ESL_OPERATOR_ROLES` with the authenticated session.
