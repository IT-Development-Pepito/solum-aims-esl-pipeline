- **Timestamp / owner:** 2026-09-02 14:00 +08:00; Claude implementation of #28.
- **Issue:** #28, host authenticated operations as Windows Service and CLI (FR-029). Selected by the owner on 2026-09-02 after #26 merged. Two decisions were put to the owner before implementation, because no document settled them: the API authentication mechanism and the source window of a scheduled run. Both recommended options were chosen and are recorded as AD-019.
- **Git state:** claude/runtime-service-api-and-cli-hosting in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\hosting, created with `gh issue develop 28` from develop at 06d83aa, the #95 merge. No schema or migration touched; head unchanged at the single `0007_audit_reconciliation`. New audit actions (`service.*`, `scheduler.*`, `reconciliation.requested`) use the existing `audit_entry` table.
- **What was built.** `domain/scheduling.py` gains `previous_cadence_instant` and `scheduled_source_window`, which walk the cron expression backwards in the schedule's timezone (bounded to 366 days, so a leap-day cadence refuses rather than spins). `runtime/scheduler.py`: `Scheduler.tick(now)` launches every due schedule through the #15 repository shape with that window, a fresh correlation id, and the active configuration and rule versions; paused it consults nothing; one schedule's exception becomes an outcome naming only the exception type. `runtime/service_host.py`: the lifecycle state machine (stopped, running, paused) the Windows Service delegates to; pause and stop quiesce scheduling first, stop bounds the tick loop by a deadline and records whether it was met, every transition is audited under the `service` actor, and a failed audit never blocks a transition. `web/auth.py`: bearer tokens from `api.token.<account>` bundle keys, compared in constant time against every token, resolved to the AD-018 principal. `web/app.py`: FastAPI routes for health (no token), runs (trigger, list, show, retry, replay), schedules (enable, disable), fallback, and scheduler pause/resume, all through `AuthorizedOperations`; 401 for no or unknown token, 403 for a role refusal already audited, 422 for a malformed request before any role check, 404 for an unknown run or schedule; OpenAPI served only to authenticated callers. `runtime/cli_operations.py`: the same operations as `esl-admin status|runs|schedules|fallback|serve`, with exit code 3 for a role refusal. `persistence/configuration_repository.py`: `ensure_active` registers the sanitized settings snapshot once per content hash. `runtime/host.py`: the composition root, with `TransactionalPorts` running each port call in its own committed transaction and `ThreadedTicker` calling the tick once a minute. `runtime/windows_service.py`: the pywin32 wrapper, importing the framework lazily; `scripts/install-service.ps1` registers it. `config.py` gains `internal_port`.
- **Evidence:** Followed superpowers:test-driven-development. RED observed first: eight new test files failed collection on the absent modules and symbols. GREEN: 102 passed across the nine new files plus the architecture boundary tests, which confirm `application` and `domain` still import nothing they must not. `python -m ruff check src tests`: two intentional naive-datetime constructions marked, otherwise clean after auto-fixes. `python -m mypy src`: no issues in 59 source files, after typing the pywin32 imports as untyped. `python -m pytest -q`: 856 passed / 9 skipped with the dedicated database, 720 passed / 145 skipped without, the CI shape. The frontend was not touched, so its gate was not run. Smoke test of the real wiring against the developer environment with the owner's `.env` exported and `ESL_OPERATOR_ROLES=Pepito=admin`: `esl-admin status` reported ready with all six dependencies `HEALTHY`, and `esl-admin runs list --store 084` read the state store through the transactional ports and answered "No runs match."; nothing was written. `esl-admin serve` and the Windows Service registration were not exercised on this machine.
- **Configuration:** `ESL_TEST_DATABASE_URL` for the suite; the owner's `.env` variables for the smoke test, exported for that shell only and never printed (connection strings were masked in the captured output). No bundle value was displayed; the smoke test read `state.password` from the owner's bundle through the same code path the service uses.
- **External state:** One branch, one worktree, and a per-worktree virtual environment created. The dedicated test database was written only inside rolled-back transactions. The smoke test read the developer state store and probed the configured SQL Server and AIMS targets read-only, as `check-connections` already does; no row was created.
- **Risks / next action:** Nothing executes a launched run: no workflow runner exists until #91 to #94 land, so a scheduled or manual execution is created `QUEUED` and stays there, and "checkpoint in-flight work on stop" has nothing to checkpoint yet. The pywin32 wrapper and the composition root are exercised by the smoke test, not by unit tests; the parts they assemble are. Restart recovery of interrupted executions (#18) is not invoked at service start and belongs with the runner. Tokens are loaded when the host starts, so provisioning or revoking one needs a restart; the WORKFLOW procedure says so. Next action: open the PR to `develop`, cross-agent review, merge once CI is green; then #29 can consume the API, and the owner can provision `api.token.Pepito` and try `esl-admin serve` against the developer environment.
