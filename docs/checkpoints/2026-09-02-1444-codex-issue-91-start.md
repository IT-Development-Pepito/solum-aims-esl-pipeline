- **Timestamp / owner:** 2026-09-02 14:44 +08:00; Codex implementation of #91.
- **Issue:** #91, read the `DBWH_8555` warehouse tier and discover stores from `DimStore`; parent epic #5; supersedes only the first tier of #11. Assigned to `it20pepito` with `type:feature`, `area:ingestion`, `area:adapters`, and `priority:p1`.
- **Git state:** `codex/91-warehouse-store-discovery` in `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-91-warehouse-store-discovery`, created from `origin/develop` at `06d83aad9bd27c84df6863a432d200baf0d7e8d9`. The root `develop` checkout was clean and at the same commit. Claude's concurrent issue work is in a separate branch/worktree.
- **Traceability:** FR-001, FR-002, FR-025, FR-026, NFR-009, AD-002, and issue #91. Dependencies #78, #10, #36, #37, and #12 are merged. Issue #38 gates later parity claims, not this read-only adapter.
- **Approved design:** Add typed application contracts plus a SQL Server adapter that dynamically reads `DimStore` and returns raw `DimItemMapping` and `FactCampaign` rows for one store. Each operation uses a bounded read transaction and records the SQL instance, database, source objects, query version, caller-supplied source window, and database read timestamp. The adapter applies only the store predicate. `LAST_MODIFIED` and `LASTUPDATED` remain raw evidence, not watermarks, because their completeness and reliability are not verified. All campaign status/type/validity/PFS predicates remain domain rules.
- **Safety:** The public port and injected SQL executor expose reads only. The concrete connection requests `ApplicationIntent=ReadOnly`, uses the #78 secret/configuration boundary, and must still be provisioned a SELECT-only SQL Server identity because connection intent is not an authorization boundary. No production system, PostgreSQL schema, SQL Server data, AIMS, Hop, Jenkins, CSV delivery, or ESL device will be modified.
- **Planned evidence:** Requirement-traceable unit tests will use a fake SQL executor and first fail against absent contracts/adapter. Optional integration coverage will run only when `ESL_TEST_SOURCE_SQL_URL` is explicitly present, must remain SELECT-only, and must never expose the URL. Adapter failures will map to the existing #20 failure-signal matrix for unreachable dependency, rejected credential, missing driver, and schema drift.
- **Next action:** Run the clean baseline, write the failing application-contract and adapter tests, record RED, then implement the smallest issue-scoped change.
