- **Timestamp / owner:** 2026-09-02 14:58 +08:00; Codex implementation of #91.
- **Scope completed to this checkpoint:** Typed `SourceWindow`, store-directory, warehouse-read, and flat provenance contracts are present in `application/contracts.py`. `adapters/warehouse.py` now discovers every `DimStore` row and returns current raw `DimItemMapping` and `FactCampaign` rows for one store inside one transaction. All emitted adapter statements are `SELECT`; business and unverified timestamp predicates are absent.
- **Documentation-first control:** `SYSTEM_ARCHITECTURE.md` records the owner-approved current-state snapshot interpretation. Issue #91's scope and acceptance wording were reconciled on GitHub: the caller-supplied window is reproducibility/audit provenance, while `LAST_MODIFIED` and `LASTUPDATED` remain raw evidence rather than extraction predicates. `PROGRESS.md` records the in-progress branch and safety boundary.
- **RED evidence:** The first focused run failed collection because `SourceWindow` and the warehouse adapter did not exist. After the initial green, the stricter audit-field test produced 7 expected failures because `WarehouseProvenance` lacked flat `source_window_start/end` and `discover_stores` did not yet accept a source window.
- **GREEN evidence:** `python -m pytest -q tests/unit/application/test_warehouse_contracts.py tests/unit/adapters/test_warehouse.py tests/integration/test_warehouse_reader.py` with this worktree's `src` on `PYTHONPATH`: 14 passed, 2 skipped. Both skips are the optional SQL Server integration tests because `ESL_TEST_SOURCE_SQL_URL` was not exported. Focused Ruff is clean after three import-order fixes. `python -m mypy src`: success in 52 source files.
- **Failure and secret safety:** Synthetic SQLSTATE tests cover unreachable (`SQL_SERVER/UNAVAILABLE`), credential rejected (`CREDENTIAL/EXPIRED`), missing ODBC driver (`CONFIGURATION/MALFORMED`), and schema drift (`SOURCE_DATA/MALFORMED`). The raised exception uses fixed signal text and never chains or renders the driver error/connection URL. The concrete factory consumes only #78 settings and `source.sql.password`; no secret field or new credential path was added.
- **External state:** Issue #91 is assigned to `it20pepito`; its existing type, area, and P1 labels remain. No database, source system, AIMS, Jenkins, Hop, CSV delivery, or device was changed. Only the issue description was reconciled with the approved design.
- **Next action:** Add the #78 factory-boundary assertion, run architecture plus full verification, inspect the complete diff for scope and secret safety, then record the delivery checkpoint before commit/PR.
