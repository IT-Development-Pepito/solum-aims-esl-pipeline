- **Timestamp / owner:** 2026-09-02 15:08 +08:00; Codex implementation of #91.
- **Implemented scope:** Typed application ports model a caller-supplied timezone-aware source window, dynamic `DimStore` routes, raw shared-tier rows, and flat provenance fields matching the existing audit model. `WarehouseReader` performs schema probes, reads database UTC time, discovers all stores, and returns unfiltered `DimItemMapping` and `FactCampaign` current-state rows for one store. The SQLAlchemy transport is isolated under `adapters/` and is supplied by #78 settings plus the fixed `source.sql.password` key.
- **Read-only boundary:** Every adapter-owned statement begins with `SELECT`; the reader has no write method and accepts no caller-supplied SQL. The connection URL requests `ApplicationIntent=ReadOnly`, and the controlled integration test separately requires that the configured identity has no `INSERT`, `UPDATE`, or `DELETE` permission on the three source objects. Read intent is not treated as a substitute for least privilege.
- **Failure behavior:** Unreachable dependency maps to `SQL_SERVER/UNAVAILABLE`, rejected login to `CREDENTIAL/EXPIRED`, missing ODBC driver to `CONFIGURATION/MALFORMED`, and schema drift/malformed source shape to `SOURCE_DATA/MALFORMED`. Exceptions contain only fixed signal vocabulary, discard the driver exception chain, and never render a URL.
- **TDD evidence:** Initial collection failed on the absent contracts/module. A second RED run produced 7 expected failures when acceptance review required flat `source_window_start/end` on every read. Final focused run including architecture boundaries: 23 passed / 2 skipped; the skips are the optional SQL Server integration tests.
- **Complete verification:** `python -m ruff check src tests` — clean. `python -m mypy src` — success in 52 source files. `python -m pytest -q` — 649 passed / 139 skipped. `frontend/npm run typecheck` — passed. `frontend/npm run test -- --run` — 1 file and 1 test passed. `frontend/npm run build` — passed, 16 modules transformed. `git diff --check` — clean. The root `.venv` was reused with this worktree's `src` explicitly on `PYTHONPATH`; frontend dependencies were reused through an ignored worktree junction.
- **Evidence classification:** Unit behavior, emitted SQL shape, port boundaries, URL construction, failure mapping, and verification results are VERIFIED. The supplied stored-procedure evidence supports the table/column names but does not establish `LAST_MODIFIED` or `LASTUPDATED` as complete watermarks; their use for incremental extraction remains UNKNOWN / NEEDS-DISCOVERY. A live source read and the configured SQL role's actual grants are also UNKNOWN because `ESL_TEST_SOURCE_SQL_URL` was not exported.
- **External state:** Issue #91 is assigned to `it20pepito` with its existing feature/ingestion/adapters/P1 labels. Its GitHub wording was reconciled with the owner-approved current-state snapshot decision. No PostgreSQL schema, production database, SQL Server, AIMS, Hop, Jenkins, CSV delivery, or ESL device was modified. No migration was added.
- **Risks / next action:** The adapter intentionally reads complete current rows for the requested store; volume must be measured before production acceptance. Run the gated integration tests with an explicitly controlled non-production URL when available. Next: commit, push, open a PR to `develop`, obtain cross-agent review, confirm CI, merge, synchronize root `develop`, and add the immutable post-merge checkpoint.
