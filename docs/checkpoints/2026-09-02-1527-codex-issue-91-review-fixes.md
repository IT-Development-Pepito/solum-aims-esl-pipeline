- **Timestamp / owner:** 2026-09-02 15:27 +08:00; Codex response to independent review of #91 / PR #97.
- **Review verdict before fixes:** Not ready to merge. Critical: the public executor yielded a generic session whose `fetch_all(statement)` accepted arbitrary SQL, so a caller could issue a write despite the reader's method names. Important: default SQL Server `READ COMMITTED` did not guarantee one transaction-level source view; `str(NULL)` produced the bogus routing value `"None"`; and substring bans did not prove the emitted SQL was exactly the approved store-only statements.
- **Resolution:** Replaced the generic SQL session capability with a closed executor exposing only `discover_stores()` and `read_store(store_code)`. The public adapter module surface is explicitly `WarehouseReader`, `WarehouseReadError`, and `WAREHOUSE_QUERY_VERSION`; no production-facing method accepts caller SQL. Concrete executor tests now assert the exact eight schema/time/data SELECT statements and their parameters.
- **Snapshot consistency:** `create_warehouse_engine` now requires SQLAlchemy's SQL Server `SNAPSHOT` isolation. There is no fallback to `READ COMMITTED`; unsupported database configuration fails safely instead of producing mixed-time evidence. `SYSTEM_ARCHITECTURE.md`, `PROGRESS.md`, and issue #91 now say so. The optional integration suite queries the active session isolation level and requires SQL Server value 5 before a live source can be considered verified.
- **Routing validation:** `ORG_CD`, `ORG_IP`, and `ORG_DB` must be non-null strings and are trimmed before entering `StoreDirectoryEntry`. Null/wrong-type/blank data maps through the existing `SOURCE_DATA/MALFORMED` signal. Unit tests cover null rejection and padded-value normalization.
- **TDD / verification:** Review regression tests failed first because the closed result types and explicit public surface did not exist. After the changes, the focused adapter/application/integration-gate/architecture run is 28 passed / 3 skipped. Ruff is clean and MyPy reports no issues in 52 source files. The three skips are controlled SQL Server tests because `ESL_TEST_SOURCE_SQL_URL` is absent.
- **External state and remaining gate:** PR #97 remains open; its two pre-review CI runs passed but are superseded by this review-fix commit and must rerun. No production or test database was contacted or changed. Next: rerun the full local gate, commit/push the fixes, publish the independent review and resolution on PR #97, then require fresh CI before merge.
