- **Timestamp / owner:** 2026-09-02 20:00 +08:00; Claude implementation of #92.
- **Issue:** #92, read the per-store iRetail tier with validated addresses and bounded fan-out (FR-001, FR-002, FR-025, FR-026). Selected by the owner on 2026-09-02 after #99 merged. Codex's #91 work was the starting point: its port shapes, executor pattern, and, after #93, the shared `adapters/sql_server` helpers.
- **Git state:** claude/92-per-store-iretail-reader in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\store-reader, created with `gh issue develop 92` from develop at c5fd2fd, the #99 merge. No schema or migration touched.
- **What the procedure does per store, VERIFIED from the decoded text.** `ORG_IP`/`ORG_DB` are read from `DimStore` per code and concatenated into four-part names. Item tier: `ITEM_MST` left-joined to `ITEM_DESCRIPTION`, predicate `ITM_STATUS = 'O'`. Campaign tier: `CMP_HDR`, `CMP_ORG_DTL` (store scope `CMP_ORG_CD`), `CMP_ITEM_GRP_HDR`, `CMP_ITEM_GRP_CND`, `CMP_CND_MST`, `CMP_ITEM_GRP_DTL`, with `CMP_STATUS = 'A'`, `CIGD_STATUS = 'O'`, today within `CMP_FROM_DATE..CMP_TO_DATE`, non-empty item, `CMP_TYPE IN (0,1,3)`, and the PFS exclusion. Stock tier: `STOCK_MASTER` (`SM_ORG_CD`, `SM_LOC_CD = '001'`), `OFFLINE_TEMP_ITEM_MOVEMENT` (`STR_ORG_CD`, `LOC_CD = '001'`), `POS_OFFLINE_TEMP_ITEM_MOVEMENT` (`STOCK_UPDATED_FLAG IS NULL`, `STR_ORG_CD`, `LOC_CD = '001'`), summed. Price tier: `BASIC_SP_MST` (`BSP_ORG_CD`, `BSP_PRICE_CATG = '001'`, `BSP_STATUS = 'A'`). Every predicate other than the store scope is a business rule (#36, #37, #12, BR-006) and is absent from the adapter's SQL; the campaign tables are joined to `CMP_ORG_DTL` solely to bound them, and the item and condition masters are read whole because the database is the store.
- **What was built.** `application/contracts.py`: `STORE_OBJECTS` (the twelve, in procedure order), `StoreReadRequest`, `StoreReadResult` with `as_mapping`/`from_mapping`, `StoreSourceReader`, `StoreReadOutcome` (exactly one of read, failed, skipped), `StoreFanOutReport` (ordered by store code, duplicates refused, `dependency_health()` as one non-required #27 line per store), and `UnroutableStore` on `StoreDiscoveryResult`. `adapters/store.py`: `STORE_SCHEMA_PROBES`, `STORE_SELECTS` (scope-only; a test asserts no business token and only `:store_code`), `SqlAlchemyStoreExecutor` (one transaction, per-statement pyodbc timeout), `StoreReader` with `from_directory_entry` going through `store_target` before any engine exists, `StoreReadError` naming the store. `adapters/store_fanout.py`: `StoreFanOut` over a `ThreadPoolExecutor` bounded by `source_store_concurrency`, `read_all` and `read_directory`, skip reasons `INVALID_STORE_ADDRESS` and `UNROUTABLE_IN_DIMSTORE`. `adapters/sql_server.py`: `HYT00`/`HYT01` map to `SQL_SERVER TIMEOUT`. `config.py`: `source_store_concurrency` (4, 1..32) and `source_store_read_timeout_seconds` (120), both provisional (NFR-004).
- **Finding that changed #91 (VERIFIED).** The first live `discover_stores` raised `SOURCE_DATA MALFORMED`: of 83 `DimStore` rows, 29 carry NULL or blank `ORG_IP`/`ORG_DB` (`001`, `015`, `0241`, `Express`, ...). The procedure skips such rows; the #91 reader failed the whole directory. `discover_stores` now reports them as `unroutable` with the missing column named and keeps reading; a row without a store code is still malformed. Codex's one test asserting the raise was rewritten to assert the report. What those rows are (closed stores, non-store organisations, data gaps) is UNKNOWN / NEEDS-DISCOVERY; `ORG_STATUS` and `MasterStatus` exist but are undocumented.
- **Evidence:** Followed superpowers:test-driven-development twice. RED for the tier: four files failed collection on the absent modules and symbols. RED for the unroutable change: four failures (two raises, two imports). GREEN: 149 passed across `tests/unit/adapters`, `tests/unit/application`, the fan-out setting tests, and the architecture boundary tests. `python -m ruff check src tests`: clean after auto-fixes. `python -m mypy src`: no issues in 64 source files. `python -m pytest -q`: 952 passed / 15 skipped with the dedicated database, 816 passed / 151 skipped without, the CI shape. The integration file was renamed to `test_store_reader_live.py` after a basename clash with the unit file. **Live, read-only, as the read-only account, with the owner's `.env` and the bundle password resolved in-process (nothing printed):** discovery returned 54 routable and 29 unroutable rows under `READ COMMITTED` (this is also #91's first successful live read); the fan-out read store 084 (`PEPITO_084`) completely in 31.5 s: 15,438 `ITEM_MST`, 15,438 `ITEM_DESCRIPTION`, 2,123 `CMP_HDR`, 2,155 `CMP_ORG_DTL`, 2,122 `CMP_ITEM_GRP_HDR`, 2,242 `CMP_ITEM_GRP_CND`, 5,429 `CMP_CND_MST`, 19,028 `CMP_ITEM_GRP_DTL`, 12,946 `STOCK_MASTER`, 0 `OFFLINE_TEMP_ITEM_MOVEMENT`, 74,924 `POS_OFFLINE_TEMP_ITEM_MOVEMENT`, 21,391 `BASIC_SP_MST`; health line `store-084 HEALTHY`. The frontend was not touched. `git diff --cached --check` reported nothing.
- **Configuration:** `ESL_TEST_DATABASE_URL` for the suite; the owner's `ESL_*` variables and `source.sql.password` for the live reads, exported for that shell only. Both `.env` examples gain the two fan-out settings.
- **External state:** One branch, one worktree, one virtual environment. Reads only: `DimStore` on `DBWH_8555`, the catalog check earlier in the day, and the twelve `SELECT`s on one store server. No row was written anywhere but the rolled-back test database.
- **Risks / next action:** The raw `POS_OFFLINE_TEMP_ITEM_MOVEMENT` read is large because `STOCK_UPDATED_FLAG IS NULL` is kept in the domain; a full fan-out over 54 stores at concurrency 4 is roughly seven minutes against a 30-minute cadence, acceptable on its face but owed a measured baseline. Only one store server has been exercised; routes to the other 53 are an operations prerequisite and each will report individually. `SELECT *` means a column added upstream reaches the raw evidence without effect until the domain names it. No workflow runner consumes the fan-out yet. Next action: open the PR to `develop`, cross-agent review, merge once CI is green; then #94 (baseline) completes the four-tier set and #21 loses its adapter blocker.
