- **Timestamp / owner:** 2026-09-02 22:00 +08:00; Claude implementation of #94.
- **Issue:** #94, read `tb_ESL` as the shadow-mode parity baseline only (FR-021, FR-022; supports the #37 deployed-parity evaluation and the #38 gate). Selected by the owner on 2026-09-02 after #100 merged. This is the last of the four issues that superseded #11.
- **Git state:** claude/94-tb-esl-parity-baseline-reader in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\baseline-reader, created with `gh issue develop 94` from develop at d4108cd, the #100 merge. No schema or migration touched; no new setting.
- **What `tb_ESL` is, VERIFIED.** The decoded DDL (UTF-16) has 42 columns keyed by `STORE_CODE, ITEM_CODE`, with `LAST_UPDATED_DATE` and `SYNC_REC` indexes; the procedure upserts every column from its `#tmpResult` after the promotion selection. It is the procedure's output, not an input, so the reader is a baseline for comparison and nothing downstream may consume it.
- **What was built.** `application/contracts.py`: `BaselineReadRequest(store_code, source_window)`, `BaselineReadResult(rows, provenance)`, `LegacyBaselineReader` port. `adapters/legacy_baseline.py`: `TbEslBaselineReader` over `SqlAlchemyLegacyBaselineExecutor` (schema probe on the key and sync columns, database-side watermark, one `SELECT * ... WHERE STORE_CODE = :store_code`, one transaction), `LegacyBaselineReadError` with #20 signals, `BaselineNotAllowed`. Two guards keep it a baseline rather than a back door: `from_settings` refuses unless `ESL_SHADOW_MODE` is true, and `test_no_ingestion_or_domain_module_imports_the_baseline_reader` walks every source module's imports and fails if any imports the adapter, so no ingestion or domain path can reach `tb_ESL`. Comparison semantics, tolerances, and mismatch classification are deliberately absent; `REDLIST` is returned as it is.
- **Evidence:** Followed superpowers:test-driven-development. RED: three files failed collection on the absent port and module. GREEN: 25 passed across the two unit files and the architecture boundary tests; one message wording was corrected so the refusal reads "shadow mode" as the test expects. `python -m ruff check src tests`: clean. `python -m mypy src`: no issues in 65 source files. `python -m pytest -q`: 969 passed / 16 skipped with the dedicated database, 833 passed / 152 skipped without, the CI shape. **Live, read-only, as the read-only account** (URL built in-process from the bundle, never printed): the integration file against the configured `ESL` database passed, and a direct read of store 084 returned 14,610 rows in 0.3 s under `READ COMMITTED` with every `REDLIST` value empty, confirming the PR #80 dead-code finding against live data. The frontend was not touched. `git diff --cached --check` reported nothing.
- **Configuration:** `ESL_TEST_DATABASE_URL` for the suite; the owner's `ESL_*` variables and `source.sql.password` for the live read, exported for that shell only.
- **External state:** One branch, one worktree, one virtual environment. Reads only: three `SELECT`s on `ESL.dbo.tb_ESL`. No row was written anywhere but the rolled-back test database.
- **Risks / next action:** The baseline is read whole per store (14,610 rows for 084) and compared later in memory; that is within the AD-005 in-memory comparison design but is another number the missing workload baseline (NFR-004) should confirm. The shadow comparison that uses this reader does not exist yet and must not claim deployed parity until #38 and representative cases are complete. Next action: open the PR to `develop`, cross-agent review, merge once CI is green; then #21 loses its adapter blocker and the ingestion path that joins the four tiers to the domain rules becomes the next engineering gap.
