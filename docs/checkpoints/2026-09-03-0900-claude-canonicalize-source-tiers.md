- **Timestamp / owner:** 2026-09-03 09:00 +08:00; Claude implementation of #103.
- **Issue:** #103, canonicalize the four source tiers into canonical records and record issues (FR-003 to FR-006, FR-025, FR-026; BR-002 to BR-018 as already implemented in #10, #12, #36, #37). Selected by the owner on 2026-09-03 as the first of the three ingestion-path issues (#103 → #104 → #102).
- **Git state:** claude/103-canonicalize-source-tiers in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\canonicalize, created with `gh issue develop 103` from develop at 7b4ae42, the #101 merge. No schema, migration, adapter, or setting touched.
- **Evidence gathered before writing a line.** The decoded procedure (UTF-16) was read region by region: the item insert (lines 340–396: `ITM_STATUS = 'O'`, the attribute-to-field mapping, `TRADING` and `00002` flag rules, `ITM_SALES_UOM` as the selling UOM), Stage A raw campaign extraction (400–503: the six-table join bounded by `CMP_ORG_CD`, `CMP_STATUS = 'A'`, `CIGD_STATUS = 'O'`, the validity window, `CMP_TYPE IN (0,1,3)`, the PFS exclusion, the description-to-group ladder, `RIGHT(CMP_DESC, 100)` as `DISC_TEXT`, `ISNULL(CIGD_UOM_CD, 'PCS')`), Stage B weekday enrichment from RUNNING `FactCampaign` rows (515–545), Stage C UOM resolution and `UOM_RULE_REQUIRED` (546–660), stock aggregation (713–770), the `ITEM_UOM_MAPPING_MST` barcode join (775–787), the category-`001` price (790–806), the Patch 2.5 zero-effect and above-regular rejections (806–850), the existing-state tie-break (850–880), the atomic promotion apply and reset (1100–1130), and the per-100GR, saving, and `DimItemMapping` min/max/display updates (1245–1278). Each predicate became one function; each is cited in the docstring.
- **What was built.** `domain/source_rules.py`: `is_active_item`, `is_active_item_mapping`, `is_selling_barcode_mapping`, `is_regular_price_row`, `is_main_location`, `is_pending_pos_movement`, `stock_on_hand`, `is_active_campaign_header`, `is_open_campaign_detail`, `promotion_type_for`, `campaign_group_for`, `disc_text_for`, `weekday_evidence_for`, `product_state_for`, `expiry_days_for`, with the issue codes for source-level exclusions. `application/canonicalize.py`: `StoreSourceBundle`, `canonicalize_store(bundle, reference_time, configuration_version, rule_version, ...)` returning records, promotion evaluations, #12 assessments, keyed issues, balanced counts, and hashes; the display page is an injected policy whose default is explicitly undefined.
- **Three things deliberately not decided, recorded in PROGRESS.** The display page (BR-007 is Hop behaviour, AIMS page semantics UNKNOWN per #24); the #37 tie-break against the existing `tb_ESL` state (the baseline is shadow-only, #94, so `existing_state=None`); and the four fields the procedure sets to constants (`MEMBER_PRICE`, `PROD_WEIGHT`, `EARLY_EXPIRY_DATE`, `NFC_URL`), which are `None` rather than invented. A risk is also recorded: `FactCampaign` can hold several rows per campaign and item, the procedure orders them by a column the supplied text truncates, and the step takes the first row deterministically.
- **Evidence:** Followed superpowers:test-driven-development. RED: both test files failed collection on the absent modules. GREEN: 80 passed across `tests/unit/domain/test_source_rules.py` (every predicate and mapping with procedure-derived cases), `tests/unit/application/test_canonicalize.py` (happy path, scalable pricing, each exclusion as an issue, missing and ambiguous regular price, one eligible campaign selected atomically, two eligible campaigns unresolved with `PROMO_PRIORITY_DIFFERENT_ECONOMIC`, non-CLR UOM unresolved, inactive/closed/unsupported campaigns excluded with issues, the window rule, the injected display policy, ordering and identical hashes, duplicate keys rejected by validation, balanced counts, and an import-graph check that the step imports no adapter, SQL, or settings), and the architecture boundary tests. `python -m ruff check src tests`: clean after marking four intentionally naive source datetimes in the fixture. `python -m mypy src`: no issues in 67 source files. `python -m pytest -q`: 1041 passed / 16 skipped with the dedicated database, 905 passed / 152 skipped without; the two warnings are starlette/anyio deprecations in this fresh virtual environment, not from this change. The frontend was not touched. `git diff --cached --check` reported nothing.
- **Configuration:** `ESL_TEST_DATABASE_URL` only. No live source was read for this issue; the step is pure and its inputs are the adapters' result types.
- **External state:** One branch, one worktree, one virtual environment. Nothing else.
- **Risks / next action:** The counts are balanced by construction (`valid = eligible + ineligible + unresolved`) but the #25 report expects more categories (`intended`, `submitted`, ...) that #104 fills. `FactCampaign` row choice and the `SOURCE` issue classification need #104's persistence to carry them. Next action: open the PR to `develop`, cross-agent review, merge once CI is green; then #104 persists snapshots, differences, issues, intended actions, and the reconciliation report from this output.
