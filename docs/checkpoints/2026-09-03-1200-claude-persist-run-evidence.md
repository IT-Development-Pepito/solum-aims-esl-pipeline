- **Timestamp / owner:** 2026-09-03 12:00 +08:00; Claude implementation of #104.
- **Issue:** #104, persist snapshots, differences, intended actions, and the reconciliation report per run (FR-010, FR-013, FR-021, FR-022, FR-025, FR-027; AD-005, AD-016). Selected by the owner on 2026-09-03 after #105 merged; second of the three ingestion-path issues (#103 → #104 → #102).
- **Git state:** claude/104-persist-run-evidence in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\persist-run, created with `gh issue develop 104` from develop at 41fe507, the #105 merge. No schema or migration touched; the step uses only tables that exist.
- **What was built.** `application/persist_run.py`: `persist_run(result, context, ...)` over six ports that are the repositories' own shapes. In order: one `SOURCE_EXPECTED` snapshot set for the execution with every record appended and finalized (#13); one processing result with its issues per record (#12) and the promotion evaluation behind it (#36); keyless exclusions as `RECORD_EXCLUDED` execution events; the store's differences against its own previous finalized snapshot, decided by canonical hash, `ADDED`/`CHANGED`/`REMOVED` recorded path by path and `UNCHANGED` counted (#13, BR-018); one `INTENDED` action per changed eligible record (#19), with `SKIPPED_IDEMPOTENT` counted where the assessment already said so; a balanced reconciliation report in `SHADOW` mode (#25) whose `eligible` is #103's eligible plus unresolved, as `validate_balance` defines the terminal categories; then, when a `tb_ESL` baseline is supplied (#94), a record-by-record comparison writing `LEGACY_BASELINE_MISMATCH`, `LEGACY_BASELINE_ROW_MISSING`, and `LEGACY_BASELINE_ROW_ONLY_IN_LEGACY` exceptions with the computed and legacy values side by side. Three checkpoints (`persist:snapshot`, `persist:actions`, `persist:report`) are appended when the runner passes a step id. Repository additions: `SnapshotRepository.find_snapshot_set` and `previous_finalized_records`; `ReconciliationRepository.append_exception` and `latest_report`.
- **Decisions and limits, recorded in PROGRESS.** Idempotency rests on the execution's own finalized snapshot set (a restart returns the existing identifiers with `resumed=True`; an unfinalized set raises `PersistInterrupted` for the runner). A keyless exclusion cannot be a `record_issue` because the snapshot foreign key is `NOT NULL`; it is an event and a rejected count, and a row of its own would need the reserved `0008` migration. `ACTIVE` mode raises `ActiveModeUnsupported` until #23. Two runs over one source window converge on one logical action by the #19 key; a later window yields a new action for a changed record, which is what the cadence guarantees and what the test shows. The baseline comparison covers regular price, promotion presence, type and text, barcode, and stock; `/100GR` is mapped back to `KGS` as the procedure does; tolerances and meaning stay UNKNOWN / NEEDS-DISCOVERY under #38 and the categories never say parity.
- **Evidence:** Followed superpowers:test-driven-development. RED: the integration file failed collection on the absent module. First GREEN run: 17 of 19 passed; the two failures were real and fixed in the step, not the tests: the stored JSONB payloads are already canonical and must be diffed directly (the canonical serializer refuses raw dicts by design), and `compared` must count every record examined. A third failure was a test artefact: both runs used the same source window, so #19 correctly returned the first run's action; the test now gives the second run its own window and says why. GREEN: 19 passed across `tests/integration/test_persist_run.py` (first shadow run, keyless exclusions as events, keyed issues persisted and enumerated, re-run without duplicates, identical input with zero differences, changed and removed records by hash, store-isolated baseline, active mode refused, baseline mismatch and missing-row exceptions without the word parity, a matching baseline recording nothing, checkpoints per phase) plus the architecture boundary tests. `python -m ruff check src tests`: clean. `python -m mypy src`: no issues in 68 source files. `python -m pytest -q`: 1052 passed / 16 skipped with the dedicated database, 905 passed / 163 skipped without. The frontend was not touched. `git diff --cached --check` reported nothing.
- **Configuration:** `ESL_TEST_DATABASE_URL` only. No live source was read.
- **External state:** One branch, one worktree, one virtual environment. The dedicated test database was written only inside rolled-back transactions.
- **Risks / next action:** `previous_finalized_records` picks the store's latest finalized set by capture time; a replay of an older window therefore diffs against the newest snapshot, not the one that preceded that window, which the runner (#102) should make explicit when replays start. The baseline comparison reads the whole `tb_ESL` store slice into memory (14,610 rows for 084, #94), within AD-005 but owed the missing workload baseline. Next action: open the PR to `develop`, cross-agent review, merge once CI is green; then #102 sequences discover → read → canonicalize (#103) → persist (#104) → reconcile with checkpoints, and the first end-to-end shadow run becomes possible.
