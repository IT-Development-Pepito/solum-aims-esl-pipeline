- **Timestamp / owner:** 2026-09-03 19:00 +08:00; Claude fix for two defects the first live shadow run exposed.
- **Issue:** None, by the owner's standing instruction for defects found in use (as with #89 and #90); the owner ran the first `esl-admin serve` with a `QUEUED` run for store 084 and sent the `runs show` output: `canonicalize:unexpected:ValueError:OPERATOR_ACTION_REQUIRED` after six minutes in that step.
- **Git state:** claude/fix-canonicalize-live-data in D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\fix-canon, branched from develop at 587c996 (Codex's `scripts/load-env.ps1`, after the #107 merge). No schema or migration touched.
- **Diagnosis, VERIFIED on the live rows.** The runner records failures by type only, so the cause was reproduced outside the run: the four tiers were read once more for store 084, read-only as the read-only account (415 s cold), saved locally under the scratchpad and never committed, and `canonicalize_store` run on them with the traceback visible. (1) `ValueError: candidate identifiers must be unique` from the #36 evidence: `CMP_ITEM_GRP_DTL` holds exact duplicate rows (same campaign, item, UOM, status, group) for 1,098 of 17,991 campaign-and-item pairs; the procedure's `SELECT DISTINCT` collapses them and the step did not. (2) The six minutes: `stock_on_hand` scanned every stock and movement row for every item, 15,444 items against 12,951 stock and 75,534 POS rows, about 1.4 billion comparisons; the failure at item order position made it look like the step's own cost.
- **Fix.** `application/canonicalize.py`: detail rows are deduplicated on (campaign, group, UOM, status) before candidates are built, mirroring the procedure's `DISTINCT`; rows of one campaign that differ by UOM stay apart with candidate id `campaign/UOM` and the same source campaign id. Stock, offline, and POS rows are indexed by item once per store in `_SourceIndex`; the domain sum `stock_on_hand` is unchanged and receives the item's slices. No rule changed.
- **Evidence:** Followed superpowers:test-driven-development. RED: `test_duplicate_campaign_detail_rows_collapse_into_one_candidate` and `test_one_campaign_with_two_uoms_for_an_item_keeps_both_candidates_apart` failed with the live error; `test_a_store_sized_batch_canonicalizes_in_seconds_not_minutes` (6,000 items against 60,000 rows, 5 s bound) was enlarged until it exposed the quadratic scan. GREEN: 75 passed in `tests/unit/application/test_canonicalize.py` and `tests/unit/domain/test_source_rules.py`, the batch test in 0.32 s. `python -m ruff check src tests`: clean. `python -m mypy src`: no issues in 70 source files. `python -m pytest -q`: 1085 passed / 16 skipped with the dedicated database, 934 passed / 167 skipped without. Re-run on the saved live rows: 1.2 s; 15,011 records from 15,444 items; 433 inactive items rejected; 4,853 promotion evaluations, 1,209 selected, 938 unresolved, 47 rejected; 11,125 issues, led by 3,891 `OUTSIDE_DATE_TIME_WINDOW`, 2,544 `DISPLAY_PRIORITY_SAME_ECONOMIC`, 1,414 `PFS_EXCLUDED`, 1,173 `WEEKDAY_INACTIVE`, 1,026 `CATEGORY_001_PRICE_MISSING`, 224 `PROMO_PRIORITY_DIFFERENT_ECONOMIC`, 161 `VALUE_BASED_CONVERSION_REQUIRED`. Recorded in PROGRESS as the first measured figures behind the open business gates. The frontend was not touched. `git diff --cached --check` reported nothing.
- **Configuration:** the owner's `ESL_*` variables and `source.sql.password` for the reproduction read, exported for that shell only; `ESL_TEST_DATABASE_URL` for the suite.
- **External state:** One branch, one worktree, one virtual environment. Reads only, against the same store the owner's run had read. The saved live rows stay in the session scratchpad outside the repository.
- **Risks / next action:** The owner's failed execution stays `FAILED` with its terminal reason; a new run must be started (`esl-admin runs start`) after this merges, since a `FAILED` run is terminal and `runs retry` is for retryable failures. `persist` and the `tb_ESL` comparison have still not executed on live data. Next action: merge, `pull to local`, restart `esl-admin serve`, start a fresh run for store 084, and read it with `runs show`; 938 unresolved items and 1,026 missing prices are business questions, not defects.
