# Checkpoint: #21 start

- **Timestamp / owner:** 2026-09-03 21:00 +08:00; Claude implementation of #21, selected by the owner.
- **Issue:** GitHub #21, [workflow] test dependency and restart recovery scenarios (FR-016, NFR-002); labels type:feature, area:workflow, area:observability, blocked, priority:p1; assigned to it20pepito. Owner decisions of 2026-09-03 are recorded on the issue: the AIMS/API mutation event is split to #113 (blocked by #23); application restart is tested at process level and server restart in-test; the recovery report is a derived read model whose presentation belongs to #109; real disruption of non-production targets is permitted. The AD-016 Task 8 items beyond this issue are split to #114 (snapshot replay), #115 (Linux database CI), and #116 (traceability matrix).
- **Git state:** `claude/21-recovery-scenarios` in `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\recovery-scenarios`, created with `gh issue develop 21 --base develop` from develop at 07f025e (the PROGRESS entry recording live shadow run `6b297d1d`). Fresh `.venv`, editable install with dev extras. No uncommitted files beyond this checkpoint.
- **Scope planned:** (1) fix the pre-existing order-dependent failure `test_runner_end_to_end.py::test_runnable_executions_are_listed_oldest_first`; (2) migration `0008_authoritative_model_gate`: preflight refusing null `workflow_schedule.configuration_version_id` by naming the rows, `SET NOT NULL`, partial unique index `(workflow_name, store_code) WHERE enabled`, and `workflow_execution.retry_not_before`; (3) runner writes `retry_not_before` on `RETRY_WAIT`, worker honours it; (4) `application/recovery.py` read model deriving scope, checkpoint, external uncertainty, and next operator action; (5) fault seams under `tests/support/`: in-process signal injection per step and a TCP forwarder to the local AIMS clone that can refuse or cut mid-read; (6) one focused integration test per FR-016 event. Non-goals: AIMS mutation (#113), replay (#114), CI (#115), docs matrix (#116), keyless-exclusion row.
- **Evidence (baseline):** `python -m pytest -q` with `ESL_TEST_DATABASE_URL`, `ESL_TEST_AIMS_PORTAL_URL`, `ESL_TEST_AIMS_CORE_URL`: 1109 passed, 7 skipped, **1 failed** (the order-dependent test above; it passes alone). Loading the full runtime `.env` instead of the `ESL_TEST_*` subset produces 7 additional failures in configuration-default tests, which is environment pollution, not a defect. `ruff check src tests`: passed. `mypy src`: no issues in 70 source files.
- **Configuration:** `ESL_TEST_DATABASE_URL`, `ESL_TEST_AIMS_PORTAL_URL`, `ESL_TEST_AIMS_CORE_URL`, read from the owner's gitignored `.env` into the process only; never printed or committed.
- **External state:** Created issues #113, #114, #115, #116 and one comment on #21. Read-only counts on the owner's development state store: 0 `workflow_schedule` rows, 0 executions with a null configuration version, 3 `configuration_version` rows, head `0007_audit_reconciliation`; so the `0008` preflight will pass there without any backfill. No production system touched.
- **Risks / next action:** The stopped-service scenario needs an elevated shell; every other fault is a non-listening port, a cut connection, or an injected signal, none of which needs elevation. Next action: diagnose the runnable-executions leak, then TDD the migration.
