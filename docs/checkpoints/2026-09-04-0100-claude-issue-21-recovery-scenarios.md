# Checkpoint: #21 recovery scenarios and the 0008 gate

- **Timestamp / owner:** 2026-09-04 01:00 +08:00; Claude implementation of #21, pre-PR.
- **Issue:** GitHub #21, [workflow] test dependency and restart recovery scenarios (FR-016, NFR-002); labels type:feature, area:workflow, area:observability, blocked, priority:p1; assigned to it20pepito. Owner decisions of 2026-09-03 on the issue: AIMS/API mutation event split to #113 (blocked by #23); application restart at process level, server restart in-test; recovery report as a derived read model with presentation in #109; real disruption of non-production targets permitted. Task 8 remainder split to #114, #115, #116.
- **Git state:** `claude/21-recovery-scenarios` in `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\recovery-scenarios`, from develop at 07f025e. Six commits: 3a7fed7 (started_at from the launch instant), af2472a (0008 gate), 764a184 (retry due time), 00f9148 (recovery report), a08bc6a (fault seams and six scenarios), plus this checkpoint with the PROGRESS/WORKFLOW/plan updates. Migration head is `0008_authoritative_model_gate`. Files: `alembic/versions/0008_authoritative_model_gate.py`, `src/esl_service/application/recovery.py`, `src/esl_service/application/runner.py`, `src/esl_service/persistence/{repository,launch_repository,action_repository}.py`, `src/esl_service/persistence/models/execution.py`, `src/esl_service/runtime/host.py`, `tests/support/{__init__,committed,sources,tcp_proxy,run_one}.py`, `tests/integration/{test_migration_0008,test_recovery_scenarios}.py`, `tests/unit/application/test_recovery.py`, and touched existing tests.
- **Scope delivered:** (1) `0008`: preflight refusing any `workflow_schedule` without `configuration_version_id` by naming it, then NOT NULL; partial unique index `uq_workflow_schedule_active_scope (workflow_name, store_code) WHERE enabled`; `workflow_execution.retry_not_before`; `execution_step.sequence` with existing rows numbered by `(started_at, attempt, id)` before the identity takes over; downgrade removes exactly these. (2) Runner writes `retry_not_before = clock + delay` on `RETRY_WAIT`, the repository keeps it only in `RETRY_WAIT`, `runnable_executions(now)` skips a retry not yet due, `step_history` orders by `sequence`. (3) `application/recovery.py`: `recovery_report` and `report_for` deriving scope, checkpoint, `resume_from`, external uncertainty, and next operator action; `unresolved_actions(execution_id=)`. (4) `tests/support`: committed-row helpers with a foreign-key-ordered purge, `ScriptedSources` with per-read faults, `CuttingProxy` (loopback TCP forwarder that refuses or cuts after N bytes), `run_one` (a runner process that `os._exit`s after a named checkpoint). (5) Seven scenario tests, one per event plus the refused-connection case. Non-goals honoured: no AIMS mutation, no replay service, no CI change, no traceability doc, no keyless-exclusion row.
- **Evidence:** Python 3.12.7. TDD throughout: every production change had a failing test first, observed (the backfill-order test was additionally proven by a deterministic mutation, `ORDER BY started_at DESC`, which it caught; the `ORDER BY id` mutation passed once by a 1-in-6 UUID coincidence, which is why the deterministic one was run). `python -m pytest -q` with `ESL_TEST_DATABASE_URL`, `ESL_TEST_AIMS_PORTAL_URL`, `ESL_TEST_AIMS_CORE_URL`: **1137 passed, 7 skipped**. `tests/integration/test_recovery_scenarios.py` alone: 7 passed in 8.4 s, including two real process deaths (exit 137) and a real mid-result socket cut on the AIMS Portal clone (`cuts >= 1`). `ruff check src tests`: passed. `mypy src`: no issues in 71 source files. `git diff --check`: clean. After the suite the dedicated test database held 0 executions, steps, snapshot sets, actions, leases, reports, schedules, and marker configuration versions, at head `0008`.
- **Findings on the way:** (a) `datetime.now()` returned 2 distinct values in 2,000 consecutive calls on this host, so launches and steps within one run tied on `started_at` and ordering fell to heap order, which an UPDATE changes; this made `test_runnable_executions_are_listed_oldest_first` and the happy-path step-order assertion flaky on `develop`. Fixed by stamping `started_at` from the launch instant and by `execution_step.sequence`. (b) During development the test database was once left stamped `0008` by an earlier version of the migration without the `sequence` column; it was repaired by hand (revert the partial 0008, stamp 0007) and the chain test then passed; nothing outside the test database was touched. (c) No run step reads the AIMS model (`fetch_labels` is unused outside contracts and adapters), so AIMS-unavailable recovery is proven by chaining the real reader's classification into a runner step; this is recorded in PROGRESS.
- **Configuration:** `ESL_TEST_DATABASE_URL`, `ESL_TEST_AIMS_PORTAL_URL`, `ESL_TEST_AIMS_CORE_URL`, test-only, loaded from the owner's gitignored `.env` into the process; never printed or committed. Loading the full runtime `.env` instead produces 7 unrelated configuration-default failures, which is environment pollution and is noted in the start checkpoint.
- **External state:** The dedicated test database was migrated 0007 -> 0008 -> 0007 -> 0008 by the chain tests and holds no leftover rows. The local AIMS clone was read as the read-only role through a loopback forwarder; no production system, SQL Server, AIMS, or the owner's development state store was touched. GitHub: issues #113-#116 created and one comment on #21 earlier this session.
- **Risks / next action:** The stopped-service case (stop `postgresql-x64-18`, one `fetch_labels`, expect `AimsUnavailable`) still needs the owner's elevated shell; every automated fault here needs none. The owner's development state store is at `0007`; its upgrade to `0008` will pass preflight (0 schedules, 0 null versions) and number the 11 existing step rows of runs `369da6c1` and `6b297d1d` by start time. Next action: open the PR to `develop`, cross-agent review, merge once `verify` is green, fast-forward local `develop`, then `alembic upgrade head` on the development state store.
