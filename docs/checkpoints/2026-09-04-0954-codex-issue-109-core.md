# Checkpoint: issue #109 core evidence surfaces

- **Timestamp / owner:** 2026-09-04 09:54 +08:00; Codex.
- **Issue:** GitHub #109; assigned to `it20pepito`; labels `type:feature`, `area:operations`, `area:web`, `area:observability`, `priority:p1`.
- **Git state:** `codex/109-run-evidence-observability` at base `e59c7de`; implementation remains uncommitted in the issue worktree; no PR yet.
- **Scope:** Shared authorized read service, read-only SQLAlchemy repository, strict response models, CLI `runs issues|report`, enriched `runs show`, API `/runs/{id}/issues|report` and enriched `/runs/{id}`, and token-protected low-cardinality `/metrics`. Existing migration head `0008_authoritative_model_gate` is unchanged. #115 CI work is untouched.
- **Evidence:** Red tests were observed for each slice before implementation: missing application module; missing repository module; unsupported `create_app(run_evidence=...)`; missing CLI seam; missing `metrics_run_limit`; and response models accepting secret-shaped evidence. Green focused gate: **70 passed, 1 warning** across the new application, schema, API, CLI, configuration, and dedicated-PostgreSQL repository tests. Ruff: **all checks passed**. mypy: **no issues in 74 source files**. Database integration proves direct `record_issue` count plus `RECORD_EXCLUDED`, latest report revision, legacy values side by side, and latest two executions independently for each of two scopes.
- **Configuration:** Added non-secret `ESL_METRICS_RUN_LIMIT` (default 20, valid 1–1000) to Settings and example environment files. Added `prometheus-client`; installed only into the existing root `.venv` for verification. The dedicated database URL was loaded from the root gitignored `.env` without printing its value.
- **External state:** The integration fixtures migrated/verified and wrote only within rollback transactions on the dedicated test PostgreSQL database. No production PostgreSQL, SQL Server, AIMS, Hop, Jenkins, CSV delivery, or ESL device was touched.
- **Risks / next action:** Full-suite regression and all frontend checks remain. The response shapes are the #29 contract. Next: review the diff, run the complete verification gate, and correct only #109 findings.

