# Checkpoint: issue #109 pre-commit verification

- **Timestamp / owner:** 2026-09-04 10:01 +08:00; Codex.
- **Issue:** GitHub #109; assigned to `it20pepito`; labels `type:feature`, `area:operations`, `area:web`, `area:observability`, `priority:p1`.
- **Git state:** Named linked-worktree branch `codex/109-run-evidence-observability`, base `origin/develop` at `e59c7de`; no commit or PR yet. Worktree provenance: `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\issue-109-run-evidence-observability`; git common directory is the root repository `.git`.
- **Scope delivered:** One authorized read service and transactional read-only repository serve CLI/API issue summaries and drill-down, latest reconciliation reports/exceptions, latest step attempts with durations and allowlisted canonicalize counts, #21 recovery guidance, and a token-protected Prometheus trend surface. Evidence is re-sanitized in the application service and validated again in strict Pydantic models. Metrics select the newest configurable N executions per workflow/store and contain no execution/item/evidence labels. Settings/examples, authoritative architecture, operator workflow, progress, and the issue plan are reconciled. No migration was added; `0001` through `0008` are untouched. #115 files are untouched.
- **Evidence:** Red-green evidence is in the 09:54 checkpoint. Final commands: `python -m ruff check src tests` — **all checks passed**; `python -m mypy src` — **no issues in 74 source files**; `python -m pytest -q` with only `ESL_TEST_DATABASE_URL` loaded from the root gitignored `.env` — **1,144 passed, 20 skipped, 1 Starlette/httpx deprecation warning in 16.50s**; `frontend npm run typecheck` — passed; `frontend npm run test -- --run` — **1 file / 1 test passed**; `frontend npm run build` — **16 modules transformed, build passed**; `git diff --check` — clean (Windows emitted only expected LF-to-CRLF checkout warnings).
- **Configuration:** `ESL_METRICS_RUN_LIMIT` is non-secret, defaults to 20, and is bounded 1–1000. `prometheus-client` is a declared runtime dependency. No secret value was printed or committed.
- **External state:** The dedicated test database was used only through rollback-protected integration fixtures. `npm ci` installed lockfile-pinned dependencies only in the ignored issue-worktree `frontend/node_modules`. No production PostgreSQL, SQL Server, AIMS, Hop, Jenkins, CSV delivery, or ESL device was touched.
- **Risks / next action:** API shapes are now the #29 frontend contract. Prometheus aggregates are rolling-window gauges, so they show the current bounded window rather than lifetime monotonic counters. Next: commit, push, open the PR to `develop`, obtain independent review and required CI, then merge and synchronize root `develop`.
