# Checkpoint: #115 start

- **Timestamp / owner:** 2026-09-04 13:41 +08:00; Codex.
- **Issue:** GitHub #115, `[ci] add a database-verify job that migrates and runs the integration suite on Linux`; assigned to `it20pepito`; labels `type:chore`, `area:ci`, `priority:p2`.
- **Git state:** `codex/115-linux-database-verify` in `.worktrees/issue-115-linux-database-verify`, created from `origin/develop` at `b0288767f1469dbabb0f97c0a486e5141785d9d8`; clean before this checkpoint; no PR.
- **Scope:** NFR-016 only. Add a self-contained Ubuntu PostgreSQL CI job, preserve the Windows `verify` job, and make `pywin32` Windows-only. The migration-graph guard joins the database job. No application schema, runtime behavior, AIMS, SQL Server, or frontend work.
- **Evidence plan:** First add a requirement-traceable CI configuration test and observe its failure; then run the focused test, the required Python/frontend gates, and GitHub Actions verification after PR creation.
- **Configuration:** CI-only `ESL_DATABASE_URL` and `ESL_TEST_DATABASE_URL` will use job-local PostgreSQL service values. No value is committed, logged, or copied to a secret store.
- **External state:** None. No database, production system, or external adapter has been contacted.
- **Risks / next action:** The Linux job must run no live SQL Server or AIMS test. Write and run the focused failing CI-configuration test.
