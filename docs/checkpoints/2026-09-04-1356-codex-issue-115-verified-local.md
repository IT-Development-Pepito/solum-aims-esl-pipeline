# Checkpoint: #115 local CI configuration verification

- **Timestamp / owner:** 2026-09-04 13:56 +08:00; Codex.
- **Issue:** GitHub #115, `[ci] add a database-verify job that migrates and runs the integration suite on Linux`; assigned to `it20pepito`; labels `type:chore`, `area:ci`, `priority:p2`.
- **Git state:** `codex/115-linux-database-verify` in `.worktrees/issue-115-linux-database-verify`, based on `b0288767f1469dbabb0f97c0a486e5141785d9d8`; uncommitted changes are `.github/workflows/ci.yml`, `pyproject.toml`, `tests/unit/test_ci_configuration.py`, and this issue's checkpoints; no PR.
- **Scope:** NFR-016. The new Ubuntu `database-verify` job uses only an ephemeral local PostgreSQL 16 service under trust authentication, sets no password and uses no GitHub secret, upgrades Alembic to head, and runs the state-store integration suite plus the migration-graph guard. `pywin32` now has a Windows platform marker. The Windows `verify` job is unchanged.
- **Evidence:** TDD RED: `tests/unit/test_ci_configuration.py` failed because `database-verify` and the platform marker were absent. GREEN: the focused test passed (2 passed). Full local verification: `python -m ruff check src tests` passed; `python -m mypy src` reported no issues in 75 source files; `python -m pytest -q` with only root `.env` `ESL_TEST_*` entries loaded into the test process reported 1208 passed, 7 skipped, 1 pre-existing FastAPI/Starlette deprecation warning in 35.96 s; frontend `npm run typecheck`, `npm run test -- --run` (1 passed), and `npm run build` passed; `git diff --check` clean. The GitHub Actions Ubuntu job remains the final external CI evidence.
- **Configuration:** Only job-local `ESL_DATABASE_URL` and `ESL_TEST_DATABASE_URL`, pointing to the ephemeral CI service, are added. No value is a production credential, GitHub secret, repository `.env` entry, or secret-store item.
- **External state:** Dedicated local non-production PostgreSQL and local AIMS clone were addressed by the existing test fixtures only; no production system, SQL Server, AIMS endpoint, or filesystem delivery was touched. GitHub has not yet been changed.
- **Risks / next action:** GitHub Actions must validate the Ubuntu service syntax and Linux dependency resolution on the PR. Commit the verified change, push, open a PR targeting `develop`, then wait for both required checks.
