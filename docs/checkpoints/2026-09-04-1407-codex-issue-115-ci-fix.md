# Checkpoint: #115 GitHub Actions database-verify fix

- **Timestamp / owner:** 2026-09-04 14:07 +08:00; Codex.
- **Issue:** GitHub #115, `[ci] add a database-verify job that migrates and runs the integration suite on Linux`; assigned to `it20pepito`; labels `type:chore`, `area:ci`, `priority:p2`.
- **Git state:** `codex/115-linux-database-verify`, worktree `.worktrees/issue-115-linux-database-verify`; committed baseline `69fe2fa` pushed in PR #123, with an uncommitted CI URL correction, its focused test update, and this checkpoint.
- **Scope:** NFR-016 only. PR #123's `database-verify` service initialization and dependency install succeeded, but `python -m alembic upgrade head` failed and the integration command did not start. The job URLs now use `${{ github.run_id }}` as a non-secret, per-job password under PostgreSQL `trust` authentication. PostgreSQL ignores it, while Alembic treats the URL as a dedicated test URL and does not resolve the absent DPAPI bundle.
- **Evidence:** **VERIFIED:** GitHub job `100928202853` completed service initialization, checkout, setup-python, and pip install successfully; Alembic was the first failing step. **INFERRED:** the empty-password URL selected the DPAPI resolution path, based on `resolve_migration_url` and its existing test that an inline test URL bypasses the bundle; the host could not decode the GitHub log archive to quote the exact exception. TDD RED: the focused configuration test failed expecting the run-ID URL. GREEN: it passed (2 passed), and its Ruff check passed.
- **Configuration:** `${{ github.run_id }}` is GitHub's job metadata, not a secret. No `POSTGRES_PASSWORD`, repository credential, GitHub secret, `.env` entry, or secret-store value is added.
- **External state:** GitHub PR #123 and CI runs only; no production, SQL Server, AIMS, or device system touched.
- **Risks / next action:** Push this focused correction and use the next GitHub Actions run as the authoritative Linux/Alembic verification. The Development-sidebar issue link still requires the GitHub UI because the available browser integration is unavailable on this host.
