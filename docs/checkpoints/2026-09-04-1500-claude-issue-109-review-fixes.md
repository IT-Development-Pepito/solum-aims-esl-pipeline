# Checkpoint: #109 takeover, independent review, and fixes

- **Timestamp / owner:** 2026-09-04 15:00 +08:00; Claude, taking over from Codex after it hit its usage limit with PR #118 open.
- **Issue:** GitHub #109, [operations] expose per-run issue, reconciliation, and step evidence for troubleshooting via CLI, API, and metrics; assigned to it20pepito; labels type:feature, area:operations, area:web, area:observability, priority:p1.
- **Git state:** Codex's work is one commit, 70a8ab9 on `codex/109-run-evidence-observability`, pushed, PR #118 open with `verify` green twice. That branch was not created with `gh issue develop`, so #109 is not linked in the PR's Development sidebar and GitHub will not link an existing branch; `claude/109-run-evidence-observability` was created linked (via `gh issue develop`) and carries the same history plus this checkpoint's commit, from which the replacement PR is opened and #118 closed as superseded. Base remains develop at e59c7de.
- **What Codex delivered, verified independently before review:** fresh venv in the worktree, `python -m pytest -q` with `ESL_TEST_DATABASE_URL`, `ESL_TEST_AIMS_PORTAL_URL`, `ESL_TEST_AIMS_CORE_URL`: 1157 passed, 7 skipped; ruff and mypy clean; `git diff --check` flagged five files ending in an extra blank line (fixed). Its checkpoints' claims held.
- **Independent review (read-only subagent, three passes over the 2,200-line diff):** Strengths: one application service behind two adapters; #21 `recovery_report` reused, not re-derived; authorization and audited refusal inherited from #26; sanitizer applied twice and failing closed; bounded metric labels with a request-local registry; integration test on the #104 persist fixture. Verdict: not ready. Critical: `/metrics` loaded every issue row and every reconciliation exception, with JSONB, for up to N runs per scope per scrape (with the first live run's 11,434 exceptions and 15,000 records, 20 runs, 54 stores, millions of ORM objects per scrape). Important: per-run reads filtered and paginated in Python; unknown execution id answered 200 with empty groups and no-report conflated with unknown; a stored secret-like key produced an HTTP 500 traceback and a CLI traceback; `runs show` authorized and queried twice with a second engine; dead step-listing code; test gaps. Minor: gauge names ending `_sum`/`_count`, `KeyError` on a partial counts mapping, `computed:`/`legacy_or_actual:` labels on non-baseline exceptions, and three items left as recorded (a `from_attributes=True` widening on the #62 schemas, a duplicated builder body now folded, and the window function's table scan to revisit with retention).
- **Fixes, TDD each (RED observed before every change):** `RunEvidencePort` now has `execution_exists`, `issue_summary_for`/`issue_page_for`, `latest_report_for` (header and counts only, carrying `report_id`), `exception_summary_for`/`exception_page_for`, and `metric_evidence` whose rows carry counts per code; the repository implements them as SQL over one `UNION ALL` of `record_issue` and well-formed `RECORD_EXCLUDED` events (a malformed event is skipped), with `GROUP BY`, predicates, and `LIMIT/OFFSET` in the statement, and the scrape as four bounded queries (ranked ids, issue counts, `DISTINCT ON` latest report counts, step rows) reading no evidence JSONB. The service checks existence first (404 "no execution with id" vs 404 "has no reconciliation report yet"), tolerates a partial counts mapping, and raises a typed `EvidenceWithheld` from the sanitizer's `ValueError`, mapped to a fixed HTTP 500 and CLI exit code 4 (`EXIT_EVIDENCE_WITHHELD`), with `LookupError` mapped to `Not found` exit 1 in the CLI. `runs show` makes one authorized call. Dead `_default_steps`/`_steps`/`execution_steps` removed; `build_run_evidence` shares a builder with `build_operations` and accepts shared ports. Gauges renamed `esl_run_step_duration_window_seconds` and `esl_run_step_samples_window`. Report labels are `computed:`/`legacy:` for `LEGACY_BASELINE_*` and `expected:`/`actual:` otherwise. The three test suites share one fake port, `tests/support/evidence.py`, instead of three copies.
- **Evidence:** `python -m pytest -q` with the test database and the AIMS clone: **1169 passed, 7 skipped**. New or changed tests: `tests/unit/application/test_run_evidence.py` (unknown id, no report, offset past end, partial counts, withheld exception evidence), `tests/integration/test_run_evidence_repository.py` (SQL summary/page parity with filters shared by both sources, offset past end, exception summary/page on the latest revision, malformed event skipped, metric counts and latest-report counts only), `tests/unit/web/test_routes.py` (404 vs 404 messages, fixed 500 without the value, renamed gauges), `tests/unit/runtime/test_cli_operations.py` (`Not found` without traceback, exit code 4, labels by category). `ruff check src tests`: passed. `mypy src`: no issues in 74 source files. `git diff --check`: clean.
- **Configuration:** `ESL_TEST_DATABASE_URL`, `ESL_TEST_AIMS_PORTAL_URL`, `ESL_TEST_AIMS_CORE_URL` from the owner's gitignored `.env`, test-only, never printed. `ESL_METRICS_RUN_LIMIT` unchanged.
- **External state:** The dedicated test database was used only through rolled-back fixtures. GitHub: linked branch `claude/109-run-evidence-observability` created; replacement PR opened; #118 closed as superseded with a comment. No production system touched.
- **Risks / next action:** The API shapes are the #29 contract and are unchanged except that unknown-run and no-report are now distinguishable 404 messages. The window function in `metric_evidence` still scans `workflow_execution`; revisit with retention or an index on `(workflow_name, store_code, started_at DESC)`. Next: cross-agent review of the replacement PR, merge once `verify` is green, fast-forward local `develop`, close #109 with the merge commit.
