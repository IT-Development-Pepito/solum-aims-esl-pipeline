# Checkpoint: #112 AIMS reader connect timeout

- **Timestamp / owner:** 2026-09-04 17:00 +08:00; Claude implementation of #112, selected by the owner after #109 merged.
- **Issue:** GitHub #112, [adapters] set a connect timeout on the AIMS compatibility reader engines; labels type:feature, area:adapters, priority:p2. Raised from the #110 finding that a closed local port on this host is dropped, not refused.
- **Git state:** `claude/112-aims-reader-connect-timeout` in `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\aims-connect-timeout`, created with `gh issue develop 112 --base develop` from develop at 079d3e0 (the #119 merge). No schema or migration touched.
- **Scope:** `Settings.aims_connect_timeout_seconds` (`ESL_AIMS_CONNECT_TIMEOUT_SECONDS`, default 10, `ge=1, le=300`), included in the sanitized configuration snapshot as every non-secret setting is. `create_read_only_engine(url, *, connect_timeout_seconds=10)` passes `connect_timeout` in `connect_args`, never in the URL; `AimsCompatibilityReader.from_settings` hands the setting to both engines. A `connect_timeout` already in the URL keeps precedence: SQLAlchemy merges `connect_args` over URL parameters, and the first cut silently replaced the #110 tests' two-second URL timeouts with the ten-second default (their run time went from 2 s to 20 s on `localhost`, which resolves to two addresses); a test now pins the precedence. `.env.dev.example`, `.env.production.example`, `docs/WORKFLOW.md` (check-connections procedure), and `docs/PROGRESS.md` updated. Non-goals honoured: the failure matrix, the retry policy, the statement timeout, and the AIMS mutation adapter (#23) are unchanged.
- **Evidence:** TDD; RED observed first (`aims_connect_timeout_seconds` refused as an extra input; `create_read_only_engine()` rejecting the keyword). New tests: `tests/unit/test_config_hosting.py` (default 10, configured value in the snapshot, 0 and 301 refused), `tests/unit/adapters/test_aims_compatibility.py` (a released local port with no URL query raises `AimsUnavailable` in under 10 s and the engine URL carries no `connect_timeout`; `from_settings` passes the configured value to both engines), `tests/integration/test_aims_compatibility_reader.py` (the clone's URL shape on a silent port, bounded by the engine argument). `python -m pytest -q` with `ESL_TEST_DATABASE_URL`, `ESL_TEST_AIMS_PORTAL_URL`, `ESL_TEST_AIMS_CORE_URL`: **1176 passed, 7 skipped in 35.7 s** (the suite had risen to 66 s while the default overrode the URL timeouts, and returned to normal with the precedence fix). `ruff check src tests`: passed. `mypy src`: no issues in 74 source files.
- **Configuration:** `ESL_AIMS_CONNECT_TIMEOUT_SECONDS` is non-secret. Test variables came from the owner's gitignored `.env`, never printed.
- **External state:** The local AIMS clone was addressed only through a released loopback port (nothing listening) and the existing read-only fixtures. No production system touched.
- **Risks / next action:** The default is provisional (NFR-004) until a connection-latency baseline exists; a value below the real handshake time on the production network would turn a healthy AIMS into a retry storm, which is why the setting is bounded and documented as never a fix for an unreachable host. Next: PR to `develop`, cross-agent review, merge once `verify` is green.
