# Checkpoint: #64 relax the links that pinned detailed evidence

- **Timestamp / owner:** 2026-09-04 22:00 +08:00; Claude implementation of #64, selected by the owner after #115 merged.
- **Issue:** GitHub #64, [persistence] allow detailed evidence purge by relaxing two action links; labels type:feature, area:persistence, area:operations, priority:p2. The `blocked` label was removed by the owner on 2026-09-04 after the migration-slot note.
- **Git state:** `claude/64-relax-action-links` in `D:\Documents\Dev\solum-aims-esl-pipeline\.worktrees\relax-action-links`, created with `gh issue develop 64 --base develop` from develop at 9d3d28d (the #115 merge). Migration slot `0009` taken as agreed; the CSV `compatibility_delivery` revision (#30) therefore becomes `0010`, which the CSV plan allows since it wrote "next revision after 0008, expected 0009" as an expectation rather than a reservation.
- **Scope delivered:** `0009_relax_action_evidence_links` makes `record_action.record_processing_result_id` and `record_processing_result.canonical_record_snapshot_id` nullable, keeping their `RESTRICT` foreign keys; the models follow; `tests/integration/conftest.py` pins the new head. `RetentionService._delete_detailed_evidence` releases the links and then deletes `record_processing_result`, `canonical_record_snapshot`, and `snapshot_set`. Architecture 5.8 replaces the IMPLEMENTED LIMITATION paragraph with what the purge now covers. Non-goals honoured: the #62 policy, eligibility guards, and disabled-by-default purge are unchanged, and no other constraint was loosened.
- **Finding that changed the scope.** The issue and architecture 5.8 both named **two** links. There are **three**: `reconciliation_exception.record_processing_result_id` is also `RESTRICT`, and a reconciliation exception is retained audit core, so it pinned the result just as the action did. It surfaced as a `RestrictViolation` on the first green run, not by reading. It was already nullable, so it needed no migration, only the same deliberate release in the purge; a test now pins that the exception survives with its category and both evidence sides intact and only the link cleared. `record_issue.result_id` is `NOT NULL` but is deleted earlier in the purge order, so it needs nothing. Recorded as VERIFIED in PROGRESS and corrected in architecture 5.8.
- **Evidence:** TDD; RED observed first (missing revision `0009_relax_action_evidence_links`; then the RestrictViolation from the third link). New tests: `tests/integration/test_migration_0009.py` (both columns NOT NULL at 0008, optional at 0009, restored on downgrade; prior-schema coverage proving rows written at 0008 keep both links across the upgrade, with committed rows purged by marker afterwards), and in `tests/integration/test_retention_service.py` the removal of results, snapshots, and snapshot sets with their counts, an action retaining its business key and idempotency key with the link nulled, and the reconciliation exception case above. `test_purge_retains_the_audit_core` no longer asserts that a record processing result survives, which was the limitation itself. `python -m pytest -q` with `ESL_TEST_DATABASE_URL`, `ESL_TEST_AIMS_PORTAL_URL`, `ESL_TEST_AIMS_CORE_URL`: **1213 passed, 7 skipped in 37 s**. `ruff check src tests`: passed. `mypy src`: no issues in 75 source files. `git diff --check`: clean. The #115 Ubuntu `database-verify` job runs `alembic upgrade head` and the whole integration suite, so `0009` and its chain test are covered by CI rather than by this machine alone.
- **Configuration:** none added. Test variables from the owner's gitignored `.env`, never printed.
- **External state:** The dedicated test database was migrated 0009 → 0008 → 0009 by the chain tests and left at head with no marker rows; every other test used the rolled-back fixtures. No production system touched.
- **Risks / next action:** `downgrade` restores both `NOT NULL` constraints and will fail if a purge has already nulled a link, which is correct and documented in the migration: the detailed row is gone and no value could be restored without inventing one, so such a database is recovered from backup. Purge remains disabled by default until a retention duration is approved (#58 territory). Next: PR to `develop`, cross-agent review, merge once both CI jobs are green.
