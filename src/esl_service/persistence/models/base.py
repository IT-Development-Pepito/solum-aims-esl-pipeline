"""Declarative base for the service-owned PostgreSQL state schema."""

from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    """Base class for the service-owned PostgreSQL state schema."""
